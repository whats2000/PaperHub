---
title: PaperHub — Software Requirements Specification
status: active
created: 2026-05-17
scope: full-system
---

# PaperHub

**A UX-first chat shell with its own paper knowledge base + RAG + slide pipeline, assembled by adapting utility functions from two reference projects.**

| Field | Value |
| --- | --- |
| Version | v2.23.2 |
| Date | May 2026 |
| Posture | **UX-first + decomposed reuse.** PaperHub owns its data layer, RAG pipeline, and slide pipeline so every step is traceable. Utility functions are *copied and adapted* from two reference projects rather than consumed as services. |
| Reference projects (utility source) | [`paper2slides-plus`](https://github.com/whats2000/paper2slides-plus) — extraction utils for arXiv / PDF / LaTeX projects + LaTeX helpers, **decomposed; not run as a service** · [`Intro2GenAI-hw1`](https://github.com/whats2000/Intro2GenAI-hw1) — chat-shell + MCP UX idioms ported to React |

---

## Revision History

| Version | Date | Summary |
| --- | --- | --- |
| v1.0 – v1.5 | 2026-05-03 → 2026-05-17 | Greenfield research-platform sweep; scope drifted to a from-scratch backend rewrite. Retired. |
| **v2.23.2** | **2026-05-29** | **Hotfix round — silent-data-loss bug in `paper_search` + render/ingest/db reliability fixes + load-bearing observability convention.** **Lead item (load-bearing): FastMCP envelope unwrap at the SS call sites.** A real-user `paper_search` turn ("Find these papers: …Global Prior Meets Local Consistency…") returned 0 hits for a paper that *does* exist on Semantic Scholar. After chasing several SS-API hypotheses (wrong endpoint, rate-limit, `.env` not loaded) — all wrong — root cause turned out to be inside our code: `MCPClient.call_tool` returns FastMCP's `structuredContent` verbatim, and FastMCP **auto-wraps** any handler declared `-> list[dict]` as `{"result": [...]}` (confirmed by direct probe: `output_schema.required = ['result']`, `wrap_output=True`). The resolver in `agents/research_pipeline.py` did `isinstance(hits, list)` on that return — evaluating False for every SS call, silently sending every title-only hit down the not-found path. Only papers with an arxiv-id continued resolving, because their fallback (arXiv-API verification) still worked. The bug stayed latent because the test stub `_StubRegistry.call("papers.search_semantic_scholar", …)` returned a bare `list(self.ss_hits)`, NOT the wrapped dict — tests green, prod silently broken. The shared unwrap helper `paperhub.agents._mcp_result.normalize_mcp_result` (used by `sql_agent` + `memory_node` for exactly this reason) was never imported by `research_pipeline.py`. **Fix**: import + apply at the 3 call sites (`papers.search_semantic_scholar` ×2 + `web.search`). Two new regression tests with a `_EnvelopeStubRegistry` matching the real wire contract; both fail before the fix (one literally trips a `_should_not_be_called` assertion proving the SS hit was dropped), both pass after. Verified live on `:8000` (run 283): the same "Global Prior" query now resolves correctly via `source='ss_by_title'` to the real `arxiv:2602.20200` paper. **Observability convention codified (commit 062e7d2)**: a new CLAUDE.md "MCP tool return-payload convention (meta-in-payload)" rule mandates that diagnostic context for any MCP call travels IN the response payload (`{"result": <payload>, "_meta": {…}}`) — not a cross-task side channel, which can't reach a caller across the MCP HTTP boundary — and that callers MUST unwrap via the shared helper. Never `isinstance(mcp_call_result, list)` on a raw return. **Other fixes shipped on this branch**: (a) **Parser cite-key downgrade** — `\cite{author2026keyword}` was tagged as a `quoted_title` and died at SS-by-title; a deterministic BibTeX-cite-key regex in `_safe_parse_request_list` downgrades it to `natural_language` so it routes through the web-search Discoverer instead. (b) **IEEE-class paper render** — `extract_arxiv_latex` matched a substring `\begin{document}` even when it was inside a `%`-commented stub line at the top of `IEEEtran.cls` papers, causing the body to render as plain text in the Citation Canvas; now skips commented matches. (c) **TikZ render** — pandoc embeds `tikzpicture` envs as `<pre>` source by default; pre-rasterise them to PNGs via `pdflatex` + PyMuPDF (DPI 200→300 for crisper fine strokes) and strip `\includegraphics` width hints that were shrinking embedded `<img>`s below their natural size. (d) **DB write serialisation** — concurrent paper adds tripped `sqlite3.OperationalError: database is locked`; serialised at the connection layer via a new `Database.write_transaction()` async context. (e) **Slides panel page-swap perf** — switching pages remounted the react-pdf `<Page>` (5s+ visible flash); now mount every page once and toggle visibility for instant swap. (f) **CLAUDE.md real-API test clarification (commit 0f3f51d)** — distinguishes the benchmark harness (sweeps / behaviour gates) from direct `:8000` calls (one-off bug repro), since the single-trace flow is the right tool when you have one failing user wording to investigate. **Versions bumped to 2.23.2** across `paperhub`, `frontend`, `paperhub-marker`. Backend 848 tests / frontend 310 tests green; ruff + mypy clean. No schema or LLM-contract change. **Test-fixture follow-up** (out of this round): the `_StubRegistry` itself should be upgraded to return the envelope shape so all existing resolver tests exercise the real wire contract — closes the test-stub-divergence class of bug at source. |
| **v2.23.1** | **2026-05-28** | **Hotfix — Docker-compose deployment: nginx 502 after backend rebuild.** Surfaced live on the containerized stack — after `docker compose build backend && docker compose up -d`, the frontend at `:8080` returned "0 sessions / no papers" because every API call through the nginx reverse proxy returned **502 Bad Gateway**. Root cause: `frontend/nginx.conf` declared the backend as a static `upstream` block (`upstream paperhub_backend { server backend:8000; }`), which nginx resolves **once at worker startup** and caches the IP indefinitely. Recreating the backend container assigned it a new compose-network IP, stranding nginx on the dead address (the underlying compose network DNS resolved `backend` correctly — `wget http://backend:8000/sessions` from inside the frontend container worked; only nginx was wrong). **Fix**: replace the `upstream` block with Docker's embedded DNS resolver (`resolver 127.0.0.11 valid=10s ipv6=off;`) plus a variable in `proxy_pass` (`set $paperhub_backend "backend:8000"; proxy_pass http://$paperhub_backend;`). The variable form forces nginx to re-resolve the hostname per request (cached 10s), so a recreated backend container is picked up automatically without restarting the frontend. **Deployment-only; no schema, app, or LLM-contract change.** Bumps the three service packages (`paperhub`, `frontend`, `paperhub-marker`) to **2.23.1** to track the release. |
| **v2.23** | **2026-05-27** | **Hotfix — Citation Canvas critical UI bugs (figure-scroll renderer crash + citation scroll-position drift).** Two browser-only defects surfaced in live use, neither catchable by the jsdom unit suite (they are real-layout/renderer behavior). **(1) Renderer crash (`STATUS_BREAKPOINT`).** Clicking a figure citation, then another citation, crashed the Chromium renderer: `HtmlView.apply()` unwrapped the previous highlight AND wrapped + scrolled to the new target in **one synchronous click flush**, mutating a heavyweight `<figure>` image block while the injected `content-visibility: auto` locked/unlocked it — tripping Chromium's display-lock CHECK. Fixed by splitting the work across a task boundary (unwrap synchronously, then defer the new wrap + scroll to a macrotask — the same approach `DeferredRemount` uses for the PDF swap) and exporting `clearHighlight`. **(2) Citation scroll lands at the wrong position.** The Canvas lays out lazily (`content-visibility: auto`) so the canvas **opens instantly**, but an off-screen block reports a 600px *placeholder* height — so `scrollIntoView` sums guessed offsets and the cited passage lands wrong (worst around figures, whose true height deviates most from 600px), and native smooth `scrollIntoView` animates toward the guessed offset and then snaps when the in-between blocks render (a "teleport"). Fixed with a self-driven glide (`findAndHighlight.scrollIntoViewStable`): each animation frame it **re-reads the target** and eases `scrollTop` 20% of the remaining gap toward centering it, so as the lazy blocks (and figure images) render and shift the target the glide simply tracks it and converges within 1px — one continuous smooth scroll that lands correctly with `content-visibility` (instant open) left untouched. Image-bearing blocks are also kept out of the lazy layout (`:not(:has(img))`) so figures get full layout. **Frontend-only; no schema or LLM-contract change** (831 backend + 309 frontend tests green). |
| **v2.22** | **2026-05-27** | **Plan F4.2 — conference-grade, customizable title page + deck-wide styling (verified on `:8000`).** Motivation: a generated deck's title page was a bare `\maketitle` with only the talk title — no authors, source, or date — and it was **uneditable** (the title isn't a content `deck_slides` row, so the F4 edit path couldn't reach it). **(1) Metadata title page.** GENERATE now fills the preamble from `paper_content` via `build_title_metadata(papers, talk_title)` (new `pipelines/slide_pipeline/title_meta.py`): `\title` = the paper's own title for a single-paper deck (talk title for multi-paper, with each paper's lead author listed), `\author` = author surnames + "et al." past three, `\date` = `arXiv:<id> (<year>)` — all **LaTeX-escaped** and **ASCII-only** (an arXiv single-paper deck compiles under pdflatex, where a U+00B7 middot is not in the OT1 font). `_enabled_papers` now loads `authors_json`/`year`/`arxiv_id`/`kind`. The title page is rendered as a **real, editable `\titlepage` frame** (not bare `\maketitle`); a new `is_title_frame` predicate excludes a leading title frame from content indexing in both `build_deck_slides` and `_real_frame_number`, so content `slide_index`→page/frame mapping (and notes) is unchanged. **(2) Two new deck-command actions.** `DeckCommand.action` gains `edit_title` (the title page — its `\title`/`\subtitle`/`\author`/`\date`/`\institute` metadata **and** the title frame's layout) and `edit_preamble` (deck-wide style — theme/colors/fonts/header-footer/global layout, the three user-requested customizations). Both edit the **page-1 block** (`get_preamble` → `edit_title_block`/`edit_preamble_block`, slots `slides_edit_title/v1` + `slides_edit_preamble/v1`, modeled on `paper2slides-plus`'s `interactive_edit_preamble` → `replace_preamble`), then a shared `_recompile_and_emit` helper recompiles (Overfull-aware), rebuilds `deck_slides`, and restores notes by `slide_index`; content frames are provably untouched (only the page-1 prefix is swapped). The classifier routes them; an `edit_slides` turn landing on page 1 (the title page) **falls back to `edit_title`** — closing the page-1 gap. The `slides_revise/v1` prompt was told to preserve the title metadata + frame across an Overfull fix. **Real-`:8000` gate passed all three flows** (generate → metadata title page, pdflatex, 0 missing chars; `edit_title` → added `\institute`; `edit_preamble` → theme→Madrid) and caught + fixed a bug unit tests missed: the new edit prompts carried literal LaTeX braces in the `user:` template, which the adapter's `str.format()` read as fields (`KeyError 'document'`) — relocated to the non-formatted `system:` block + added a render-the-real-template regression test. Surfaces touched: §III-5.3 (GENERATE title metadata + two new sub-flows + page-1 fallback), §III-7 (no schema change — title frame stays out of `deck_slides`), FR-12 (title-page + style customization affordances). Implementation: **plan F4.2** (`docs/superpowers/plans/2026-05-27-paperhub-F4.2-slide-style-customization.md`). |
| **v2.21** | **2026-05-25** | **Plan F4 redesign — decouple slide generation from speaker-note generation; the deck becomes a living, editable artifact.** Motivation: a real-API test of the F3 generator surfaced a class of UX failures rooted in F3's *bundled, one-shot, notes-always* design. Asking "把講稿變成繁體中文嗎" (just re-language the speaker notes) triggered a **full deck recreation**, because `sl_draft` welds the frame and note into one `SlideDraft` and `sl_notes_finalize` runs on every successful compile — there is no way to touch notes without regenerating slides. Three further gaps: **no length control** (page count was emergent from `pdflatex`, the user's "20-minute talk" never entered the pipeline), **note language locked to slide language** (both read the one session `response_language`), and **no edit path at all** (GET-only REST, no graph edit branch, notes a single `speaker_notes_json` blob). The redesign makes a deck a living artifact with independent, opt-in operations: **(1) Decouple slides ⟂ notes.** The GENERATE path produces **slides only** — `sl_draft` becomes frame-only (`SlideDraft.note` removed) and `sl_notes_finalize` leaves the generate path. Speaker notes are a **separate, opt-in flow** run against the finished deck. **(2) Deck-scoped sub-router** (the routing decision). The top-level router keeps its single `slides` intent; `report_graph` gains a **front classifier that runs only when a `decks` row already exists for the session**, emitting a `DeckCommand {action ∈ generate_notes | edit_notes | edit_slides | regenerate, target ∈ current | page:N | all, note_language?}`. It resolves "把講稿變成繁體中文" → `edit_notes / all / zh-Hant`, "改第三頁" → `edit_slides / page:3`, "幫我加講稿" → `generate_notes`, "重做整份" → `regenerate`; no deck → the GENERATE path. **(3) Opt-in notes + independent note language.** Notes default to **absent**; the GENERATE finalize message stops being a bare "Generated a 15-slide deck" and instead **hints the next moves** (generate notes · edit a slide · edit the deck). Note language is **independent** of slide language (`note_language` carried per slide + on the NOTES/EDIT flows), so a deck stays in its papers' language while the notes become Traditional Chinese — the exact failed scenario. **(4) Per-slide rows.** New `deck_slides` table `(deck_id, slide_index, frame_tex, note_text NULL, note_language NULL, page_start, page_end)` so one slide/note edits without re-serializing the deck and diff-edits are clean; `decks.speaker_notes_json` becomes a **derived cache** (keeps the `deck` SSE `has_notes` + `GET /deck` back-compat). **(5) Length control.** Default **20 min ≈ 15 slides** (`slides ≈ minutes × 0.75`, clamped 8–30) + a `depth ∈ {overview, standard, deep}` knob; the user's "20-minute talk" / "15 slides" flows into `sl_narrate` as a real slide budget (was emergent). **(6) Style discipline = `paper2slides-plus` content contract on `metropolis`.** Keep `\usetheme{metropolis}` (cleaner than the reference's Berlin/dolphin); adopt the reference's *content* rules into the prompts — slide 1 = title / first-author / date / arXiv-url; slide 2 = executive summary; intro → method (the bulk) → results (quantified, e.g. "14% better") → conclusion; ≤4 bullets/frame, ≤12 words each; ≤1 figure/frame, only from the verified inventory. **(7) Edit = agent diff-edit** (single frame or single note) + recompile, **never full regen** (reuses `paper2slides-plus` `beamer_helpers` frame extract/replace + `VersionHistory`). This **re-scopes Plan F4**: the old "presentation + editing" F4 absorbs decoupling + opt-in notes + independent note language + the `deck_slides` schema + length control **on top of** its editing / version-history / presentation tasks (presentation mode + `BroadcastChannel` + Q&A-during-talk choreography unchanged). **Surfaces touched:** UC-4 (opt-in notes + length + edit choreography), FR-12 (deck-command affordances, length input, note-language independence), §III-3 Report Agent row, §III-5.3 (three sub-flows — GENERATE / NOTES / EDIT — + deck-command classifier + length budget + note decoupling), §III-7 (+`deck_slides` table; `speaker_notes_json` derived). Implementation: **rewritten plan F4** (F3's generation pipeline stays; F4 adds the decoupled NOTES/EDIT flows, the classifier, the schema, the length budget, and the style-discipline prompt tightening). |
| **v2.20** | **2026-05-24** | **Plan F2.1 — Marker as an optional, asynchronous add-on (UX + resilience hardening of the F2 ingestion path).** Motivation: a real-workspace migration of existing PDFs surfaced that **Marker is far too slow to sit on the upload's synchronous path** — VRAM use scales with page *content density*, not page count, so a single dense two-column page (e.g. a medical-journal article) produces **200+ Surya OCR text lines** that saturate a 6 GB GPU; a 5-page batch was measured at **21 min**, and batching >1 such page tips into the catastrophic CUDA shared-memory fallback. v2.19's synchronous Marker ingest would block an upload for tens of minutes. **The fix re-frames Marker as a preferred-when-present add-on, never a hard dependency** (the same optional-external posture as `pandoc` / `open-websearch`): **(1) Synchronous live ingest is PyMuPDF-only and always works** — a PDF upload extracts text + font-size heading sections (PyMuPDF), chunks + embeds, renders HTML, writes a *degraded-but-instant* `PaperAsset` (embedded/page images as figures, heading sections, **no** equation LaTeX), and **returns in seconds**. The whole app (chunking/agentic-RAG retrieval, Citation Canvas, F3 slides) is fully functional on this baseline alone — even if Docker / the `marker` service is down. **(2) Marker runs as an eager, durable background upgrade** — on every PDF ingest (PDF upload *or* arXiv-via-PDF fallback), IF the `marker` service is reachable, the paper is marked `asset_status='marker_pending'`; a **single sequential in-process worker** (one paper at a time → no GPU OOM; `PAPERHUB_MARKER_MAX_PAGES` default **1**) drains pending papers, **re-extracts via Marker → upgrades the on-disk `PaperAsset` to Marker quality (real cropped figures + adjacent captions + equations→LaTeX) AND re-chunks + re-embeds from Marker's cleaner structure** (better agentic-RAG retrieval), then sets `asset_status='marker_ready'`. The worker **resumes** any `marker_pending` rows on backend startup, so a `--reload` or crash mid-job never strands a paper; Marker failure → `asset_status='marker_failed'` (the PyMuPDF baseline is retained, still functional). **arXiv with e-print source is unchanged** — LaTeX extraction is both fast *and* highest-fidelity, so it stays synchronous (`asset_status='latex'`), never enqueued. **(3) Consumers prefer Marker when ready, degrade gracefully otherwise** — `read_paper_asset` is unchanged (reads whatever is on disk); F3 slides + RAG automatically benefit once a paper flips to `marker_ready`, and produce usable output from the PyMuPDF baseline in the meantime. New `paper_content.asset_status` column (`'latex' | 'pymupdf_only' | 'marker_pending' | 'marker_ready' | 'marker_failed'`). The `paperhub-backfill-assets` operator CLI (added this round) backfills pre-F2 papers — Marker for PDF/arXiv-via-pdf sources, LaTeX for arXiv source; idempotent; **strictly sequential** (concurrent conversions OOM the GPU). Client read timeout 600s→1800s for the worst-case single contended page. Surfaces touched: §III-5.1 (two-tier extraction + `asset_status` + background worker), §III-6 (`marker` posture: optional add-on), §III-7 (`paper_content.asset_status`). Implementation: **plan F2.1** (lands before F3 consumes the asset). |
| **v2.19** | **2026-05-23** | **Plan F quality redesign — Marker-based structured ingestion + PhD-grade slide agent. Authorized to nuke the unmet F1 generation design.** Motivation: a real-API test of the F1 deck generator produced conference-UNusable output — hallucinated/nonexistent `\includegraphics`, wrong figures (cross-paper filename collision), overflowing dense frames never corrected (the `_revise` was a no-op + Overfull is a *warning* pdflatex ignores), and incomplete speaker notes (`\pause` made PDF pages outnumber logical frames). Root issue: crude PyMuPDF `img_N` figure extraction + blind per-section generation + no self-correction. The redesign is **two new Plan F phases** (renumbered: the old F2 presentation+editing plan becomes **F4**; this work is **F2 + F3**): **(F2) Marker-based ingestion.** A new docker-compose **`marker`** service (FastAPI wrapper around [`datalab-to/marker`](https://github.com/datalab-to/marker)'s `PdfConverter`, GPU-optional, models baked in) becomes the **PDF ingestion engine** — replacing PyMuPDF `extract_images_from_pdf`. It returns a structured document (blocks with `section_hierarchy`; `BlockTypes.Figure/Picture` = cropped figure images **+ adjacent caption blocks + page bbox**; `BlockTypes.Equation` = math **rendered to LaTeX** via Surya/texify; tables). This is the first compose service of the project's planned full-Docker move. **Two extractors, one contract:** **arXiv** papers with an e-print archive keep the LaTeX-source path (real vector figure files + exact `\caption` text + exact equations — highest fidelity, mostly already built); **PDF-only** papers (uploads, or arXiv with no source) go through the `marker` service. Both normalize to a unified **`PaperAsset`** persisted under `papers_cache/<key>/`: `assets/figures/fig-NNN.png` + `figures.json` (`{id, caption, page, section}`), `equations.json` (`{id, latex, section}`), `structure.json` (ordered sections→blocks). RAG chunks are (re)derived from the structured blocks (cleaner sections), the Citation Canvas reads the Marker HTML for PDFs (LaTeX→HTML stays for arXiv), and the slide agent reads the figure/equation inventory. **(F3) PhD-grade slide agent.** Nukes the F1 `plan → blind parallel sections → assemble → no-op revise → notes` chain and rebuilds `agents/report_graph.py` to simulate how a PhD builds a conference talk: `sl_resolve → understand` (per paper, flagship: contribution/method/results + which REAL figures (by inventory id) + key equations) `→ narrate` (flagship: ONE synthesis storyline; per-slide `{goal, key points, assigned real figure id OR LaTeX equation, source chunks}` — figures assigned ONLY from the `PaperAsset` inventory) `→ draft` (per-slide fan-out: a CONCISE Beamer frame ⟂ a RICH speaker note generated as a PAIR) `→ coherence` (flagship holistic pass over ALL frames: dedup, flow, consistent style, density discipline) `→ assemble` (preamble + theme + merged `ADDITIONAL.tex` macros + `\graphicspath` to staged real figures) `→ verify_figures` (**DETERMINISTIC**: every `\includegraphics` must reference a staged inventory id; unknown → revise/re-draft that frame, never silently render — the hard no-hallucinated-figure guarantee) `→ compile_revise` (**LOOP**: pdflatex; on fatal error OR **Overfull-vbox / off-slide overflow** → flagship revises the offending frame(s) with the log → recompile until clean — the reference's revise stage generalized to treat overflow as a fixable failure) `→ notes_finalize` (one rich note per FINAL page; reconcile to page count; the draft prompt forbids `\pause` so PDF pages == logical frames) `→ emit`. **Three hard quality contracts:** (1) **concise slides / rich notes** — frames ≤~4 bullets, ≤~12 words each, carrying key points + at most one assigned figure/equation; the *detail* lives in the speaker note (the spoken narrative); (2) **no non-existent figures, ever** — enforced deterministically by `verify_figures`, not prompt hope; (3) **self-correcting layout** — Overfull-vbox is a failure the compile loop fixes (split/tighten), not an ignored warning. **Cost is no object; outcome quality is everything** (flagship throughout, multi-pass, unbounded-ish revise). **Reused from F1 (NOT nuked):** the `decks` table, deck REST endpoints, the Slides panel + deck chip (incl. the F1.x filmstrip-resize), `compile_with_revise`'s `asyncio.to_thread` threading. **Nuked/rewritten:** the F1 `slides_plan/section/notes` prompts, the crude `slide_pipeline/figures.py` stem approach, the `_revise` no-op. Surfaces touched: §III-5.1 (Marker ingestion + `PaperAsset`), §III-5.3 (PhD slide-agent rewrite), §III-3 Report Agent row, §III-6 (`marker` compose service), §III-7 (PaperAsset on-disk layout). Implementation: **plan F2** (Marker ingestion) then **plan F3** (slide agent), F2 first since F3 consumes its output. |
| **v2.18** | **2026-05-23** | **Plan F design — Slide Pipeline + Report Agent; the deferred render-framework decision is resolved and the slide experience is fully specified.** **(1) Framework locked = Beamer-via-LangGraph** (§III-5.3 step-4 deferred since v2.1). Rationale: conference-ready academic output demands lossless math, real `\includegraphics` figures, and tikz/pgfplots — Marp/Slidev (Markdown + KaTeX/Mermaid) are toy-grade for a conference talk. Cost accepted: **`pdflatex` becomes a hard dependency** for the `slides` intent (was optional, for the Canvas, via pandoc), and the compile-fix retry loop is load-bearing. The copied `paper2slides-plus` LaTeX helpers (`latex_utils` / `compiler` / `beamer_utils` / `history` + speaker-note gen) become load-bearing rather than dead weight. **(2) Multi-paper transform.** The reference's single-paper, single-LLM `initial→update→revise` chain becomes a **traced multi-paper LangGraph subgraph** (`agents/report_graph.py`): `sl_resolve` → `sl_plan` (flagship: one **thematic, cross-paper synthesis/survey** outline `SlidePlan` over all enabled papers' abstracts + `sections_json`) → `sl_sections` (`asyncio.gather` fan-out, one retrieval-grounded LLM call per planned section, chunks pulled from that section's source papers) → `sl_assemble` (deterministic: preamble + merged-deduped `ADDITIONAL.tex` macros + `\graphicspath` across every contributing paper's `papers_cache` source dir) → `sl_compile` (reused `try_compile_with_fixes` revise loop) → `sl_notes` (one speaker note per PDF page) → `sl_emit`. A single enabled paper degrades to a clean paper-summary deck. **(3) Persistent, editable, session-scoped deck.** New 9th table `decks` (one current deck per session, `UNIQUE(session_id)`; tex/pdf paths, `speaker_notes_json`, `plan_json`, `page_count`, `theme`, `contributing_paper_ids_json`, `status`). The agent knows whether a working deck exists and `sl_resolve` (LLM) classifies `create` / `recreate` / `edit`; on `edit` the **LLM decides page-vs-deck scope** informed by `current_view_page` (the slide on screen) — not a user toggle. **Diff edit, never full regen:** deck edit → LLM patch plan → regenerate only affected frames + splice the rest (`beamer_utils.replace_frame_in_beamer`); page edit → single-frame rewrite + speaker-note shift (reuse reference logic). **(4) Version history = parity with `paper2slides-plus/src/history.py`** (NOT a new diff-revert): snapshot `(tex, speaker_notes)` bundles under `chat_session/<sid>/slides/edit_history/` per successful compile; list + restore in lockstep, keyed by `session_id`. **(5) Slide UI (FR-12, new).** A new right-panel canvas (shared-slot rule with Canvas + Memory): **filmstrip navigator + current slide (react-pdf) + collapsible speaker note**, with a **draggable horizontal divider** between slide and note. A deck chip in the assistant message opens it; generation progress streams over existing `tool_step` events. **(6) Presentation mode.** Toggling Present **keeps the chat + slides panel mounted** and enriches the panel into a presenter view (timer + next-slide preview + sync badge); opens a **separate fullscreen window** `/present/<sid>` that is **slide-only, zero chrome** on the second screen. Page sync via **`BroadcastChannel('paperhub-present-<sid>')`** — every page switch broadcasts `{page}`; the audience window holds its own state, so closing the panel for an audience-question Q&A (normal `paper_qa` + Citation Canvas) leaves it untouched and `current_view_page` is persisted. No new backend for Q&A-during-talk. **(7) SSE/REST.** New `deck` SSE event; new `api/decks.py` (`GET /sessions/{id}/deck`, `/deck/pdf`, `/deck/tex`, `/deck/versions`, `POST .../versions/{file}/restore`). **(8) Tracing** per the observability policy: `report:resolve`/`plan`/`section[i]`/`figure_path_rewrite`/`compile`/`edit_plan`/`edit_frame`/`notes`/`emit`. **Surfaces touched:** UC-4 (full experience), §III-3 Report Agent row + `AgentState` deck fields, §III-5.3 (framework locked + subgraph + decks + history), §III-7 (8→9 tables; `decks` DDL; `chat_session/<sid>/slides/` tree), new **FR-12**. A static UI mockup lives at `docs/superpowers/mockups/plan-F-slides-ui.html`. **Implementation lands as two per-phase plan files** (Phase 1 generation+viewing; Phase 2 presentation+editing+Q&A choreography). |
| **v2.17** | **2026-05-22** | **Plan E extension — Memory governance + Memory Manager UI + library_stats=paper_content fix.** Three additive layers on the same branch (`feat/plan-E-library-intelligence`): **(1) Memory governance.** A deterministic rule **Memory Gate** (`agents/memory_gate.py`) runs BEFORE any `memory.add` — whether triggered explicitly by the `memory` node or autonomously by `paper_qa`/`library_stats`. It refuses two classes of content: **sensitive** (API keys matching `sk-...`/`AIza...`/similar, passwords, credit-card or ID numbers, medical/diagnosis text, salary figures, customer PII) and **dangerous** ("skip validation", "disable security", "ignore rules", "bypass review"). Refusal returns a visible reason to the user and is traced as `status='rejected'` (NFR-05). Rules own safety boundaries per §II-1 / §II-2 — an LLM classifier would have non-zero false-negative rate. Allowed content continues to the LLM scope classifier, which maps **personal preferences/habits → `global` (user scope)** and **project/framework/DB/architecture settings → `session` (project scope)**. Note: `session` scope == project-scope memory (facts tied to this chat/project context, e.g. "this project uses FastAPI"); `global` scope == user-scope memory (personal, cross-session preferences, e.g. "always answer in Traditional Chinese"). The v2.16 visibility values ARE the functional "user vs project" distinction — no rename is needed. A **status lifecycle** is added to `memories`: a new `status TEXT DEFAULT 'active' CHECK(status IN ('active','superseded'))` column, plus `supersedes INTEGER NULL` (the old memory id this replaced) and `superseded_by INTEGER NULL` (the new memory id that replaced this one). On every `memory.add`, a small LLM call compares the incoming content against all **active, same-scope** memories; if it contradicts or replaces one, the new memory is saved with `supersedes=<old-id>`, and the old row is flipped to `status='superseded'` + `superseded_by=<new-id>` (e.g. "switch to FastAPI" supersedes "use Flask"; "answer in Traditional Chinese" supersedes "answer in English"). **Active-only recall:** `recall_memories`, `build_memory_context_block`, and the `memory` MCP `recall` all filter to `status='active'` — superseded memories are never injected or surfaced by agents. **(2) Memory Manager UI.** A REST surface (NOT MCP — deterministic curation, agents do not reactivate memories) mirrors the `papers` PATCH/DELETE pattern: `GET /memories?session_id=<id>` (lists the session's session-scoped + all global memories, BOTH active and superseded, with scope/status/created/updated + supersedes/superseded_by id links), `PATCH /memories/{id}` (edit content AND/OR status — re-activate a superseded note or deactivate an active one), `DELETE /memories/{id}` (forget), optional `POST /memories` (manual add from UI). The frontend gains a **Memory Manager panel** (`components/chat/MemoryManager.tsx` + api client additions + a store slice) that mirrors the Reference Sources drawer/panel: memories grouped by scope (Session / Global), active/superseded badges, supersede-chain display, per-row controls to edit content, delete, or toggle active↔superseded. Opened via a Composer or sidebar control. **(3) library_stats `paper_content` fix + schema-awareness.** "My library / all my papers" asks must query `paper_content` (the full deduplicated cross-session paper index), NOT `papers WHERE session_id` (that is only THIS chat's membership rows). The SQL planner prompt (`sql_planner_v1.yaml`) is updated to distinguish: library-wide questions → `paper_content`; "this chat's references" → `papers ⨝ paper_content WHERE papers.session_id = :session_id`. The planner is also seeded with the real column schemas of `paper_content` and `papers` (titles/abstracts live in `paper_content`, not `papers`) so it stops hallucinating non-existent columns such as `summary` or `title` on `papers`. FR-10 and UC-7 extended; new FR-11 (Memory Manager UI); §III-3 Memory node + §III-6 memory MCP + §III-7 DDL updated; Part-0 matrix gains a Memory Manager row. |
| **v2.16** | **2026-05-22** | **Plan E — Library Intelligence + Memory. Two coupled subsystems on one revision/branch (`feat/plan-E-library-intelligence`, two waves): (Wave 1) the long-stubbed `library_stats` intent gets a real SQL Agent backed by a new in-process read-only `sql` FastMCP server; (Wave 2) a session+global Memory store with its own write-capable `memory` FastMCP server, fully separate from `sql`.** Motivation: `library_stats` has been a `_stub_library_stats` node since Plan A, and the SRS-original "NL2SQL via sqlite MCP" + "every tool via MCP" promises (Part 0, UC-5, acceptance I-8 #4) were unmet. **Wave 1 — SQL Agent + `sql` MCP.** A second in-process **FastMCP** sub-app named `sql` (mounted at its own path alongside the `paperhub-papers` server, registered as one `[[server]]` block in `mcp_servers.toml`, reached over loopback by the agent's `MCPClient`) exposes three **read-only** tools: `sql.list_tables()`, `sql.describe(table)`, `sql.query(sql)`. `sql.query` is the **hard safety boundary** (II-1 #3, NFR-05): `sqlglot.parse_one` → verb allowlist `{SELECT, WITH}` + table-name allowlist `{paper_content, papers, chunks, chat_sessions, messages, runs, tool_calls}` (NOT `memories` — kept fully inside the `memory` MCP); any violation returns a rejection AND writes its `tool_calls` row with `status='rejected'` (this is the first reachable `rejected` path — closes Plan B known-follow-up #2, the `RejectionPill`). A statement timeout + row cap guard runaway queries. The **SQL Agent** replaces the stub: a small-model introspection-driven loop (`list_tables` → `describe` the relevant tables → emit a `SELECT`), executes via `sql.query`, **self-repairs once** on error/empty (re-prompt with the error + schema), then a flagship call phrases the answer in `response_language` with the executed SQL embedded as a ```sql block (UC-5 "executed SQL shown"). Slots `sql_planner/v1` + `sql_repair/v1` (already reserved §III-4). **Read-and-act (UX-first):** when the ask is "find papers I already have on X", the agent shortlists matching `paper_content` rows as `SearchCandidate`s (`paper_id="library:<paper_content_id>"`), marks ≤2 with `finalize:true`, and the chat endpoint **auto-attaches** them via `add_paper_to_session_dispatch` (always a cache-hit — already in the library) before emitting the **same `search_results` SSE event** — so the existing `SearchResultList` frontend renders auto-added badges + Add buttons for the tail, with **zero new frontend component**. **Wave 2 — Memory.** A new 8th table `memories (id, scope CHECK IN ('session','global'), session_id NULL→chat_sessions.id, content, created_at, updated_at)` (idempotent column-add migration; `session_id` NULL ⇔ `scope='global'`). Memory is **fully self-contained in its own in-process `memory` FastMCP server** — the *only* write-capable MCP surface in the system: `memory.recall(query, scope)` (FTS5 over `content`, scope filter), `memory.add(content, scope)`, `memory.edit(id, content)`, `memory.forget(id)`. **Scope enforcement is the NFR-05 boundary for writes:** a `scope='session'` write binds to the request-context `session_id` (the `X-Paperhub-Session-Id` header the FastMCP middleware already injects); `edit`/`forget` are **ownership-checked** — the target must be `global` or belong to *this* session, else the call is rejected and the `tool_calls` row is `status='rejected'`. **Writes have two triggers:** (a) **user-explicit** — the router gains a sixth active intent `memory` (FR-06) routing "remember… / update my note… / forget…" to a small memory node that recalls→decides→writes; (b) **agent-autonomous** — `memory.add` + `memory.recall` are also tools available to `paper_qa` and `library_stats` so an agent can persist a salient durable fact mid-turn (capped ≤2 `add`/turn). **Recall injection is on by default** into the `paper_qa` finalizer and the `library_stats` agent: a `memory_recall` step pulls top-k relevant session+global memories (cheap FTS) into a labeled context block ("Relevant remembered facts:") so answers are memory-aware; semantic recall (embed memories → Chroma) is the documented opt-in **upgrade path**, off by default. **Observability:** every SQL/memory step obeys the agent-flow record principle (record the emitted SQL, rows returned, repair attempt, memory ids recalled/written). **DuckDB is dropped** from the SRS entirely (the demo's fixture set is small enough that plain SQLite handles every `library_stats` prompt; the opt-in OLAP path was speculative and unused — removed from Part 0 + §III-7 rather than carried as dead scope). Surfaces touched: Part 0 matrix (NL2SQL + MCP-scope rows now shipped; new Memory row), UC-5 (read-and-act), new **UC-7** (memory), FR-06 (6 active intents + `memory`), new **FR-10** (Memory store), §III-3 (SQL Agent row rewrite + Memory note + AgentState fields), §III-6 (new `sql` + `memory` MCP rows; the v2.6 "Plan E owns only the sqlite MCP server" note is rescinded — Plan E now owns `sql` + `memory`), §III-7 (schema 7→8 tables). The implementation plan (`docs/superpowers/plans/…-E-…md`) carries the per-task TDD breakdown. |
| **v2.15** | **2026-05-22** | **Cross-device session sync — the backend DB is the single source of truth for the session list AND each session's message record (both were browser-localStorage-only, so a chat was invisible on other devices and a stale local id crashed the chat endpoint).** New surfaces: `GET /sessions` lists *meaningful* sessions (≥1 message OR a non-default title; untouched 'New chat' empties excluded as clutter), `GET /sessions/{id}/messages` replays history (content + the run's routing decision + persisted paper-search cards), `POST /sessions/{id}/restore` undoes a delete. Frontend `useSessionsSync` **strictly mirrors** the DB — adds listed sessions, prunes any local session whose backend row is gone (incl. a locally-cached copy of a chat deleted on another device), keeping only unsent drafts — and **re-syncs the active session's messages from the DB on every activation** (replace, skipping an in-flight streaming turn so live state isn't clobbered). **Delete model:** authoritative + immediate — empty+unnamed sessions hard-delete; *meaningful* ones **soft-delete** via a new `chat_sessions.deleted_at` tombstone (Undo = restore, propagates to every device on next load); `purge_deleted_sessions` reclaims tombstones older than `PAPERHUB_SESSION_RETENTION_DAYS` (default 30) at startup. **Crash fix:** a stale client `session_id` (reset workspace / deleted session) no longer FK-crashes `_new_run` — `_ensure_session` uses `INSERT OR IGNORE`; the first user message persists the session title backend-side. **Cards persist:** paper-search result cards are stored per turn on `runs.search_results_json` and replayed, so they show on every device (the dev-only trace stays streaming-only — cards are the user-facing record, trace is debug). **Boot banner:** the backend prints an iconic 'ready' banner once the whole stack (DB, vectors, model server, MCP) is wired AND model warm-up resolves, so the UI's transient connect-while-booting errors aren't mistaken for a failed boot (TTY → ANSI + Unicode box, else plain ASCII; never breaks boot). Schema: `chat_sessions.deleted_at` + `runs.search_results_json` (both idempotent column-add migrations). No LLM-visible contract change. |
| **v2.14** | **2026-05-21** | **Citation Canvas evolved from a single-chunk overlay into a right-side reference *reading panel* (push layout, paper switcher, PDF/HTML auto-select, dark-mode, keep-alive cache); stale-chunk 404 handled gracefully.** Implemented in Plan D Wave 2 + Wave 3 after live UI testing. Changes: **(1) Push layout** — the panel is an animated CSS-grid column that pushes the chat (mirrors the left sidebar), not a `fixed` overlay; toggled by the composer **References** button (the former "Coming in Plan D" placeholder). **(2) Paper switcher** — a tab per *enabled* session reference, overflowing to a "…" dropdown beyond 3; clicking a tab browses that paper, clicking a `[chunk:<id>]` citation opens+switches+highlights, and a browse-mode open (References button) shows the active paper with no highlight (the resolved citation is consumed so re-opening doesn't re-jump). **(3) PDF vs HTML auto-select** — `kind` is unreliable (arXiv source-archive fallback yields `kind='arxiv'` with a PyMuPDF-rendered, visually-broken HTML), so a new `GET /papers/content/{id}/document` probes for a top-level `*.pdf` in the cache dir and returns `{mode}`; PDF-rendered papers are shown via a new `GET /papers/content/{id}/pdf` (inline `FileResponse`) in the iframe, HTML papers via the existing `/html`. Highlighting is HTML-only (PDF native viewer has no searchable DOM — consistent with §I-7 "no PDF.js"); a PDF citation shows an inline note. **(4) Dark mode** — the iframe document gets an inversion stylesheet injected (`applyIframeTheme`) in the app's dark theme, re-applied on theme toggle (HTML-only). **(5) Keep-alive cache** — each visited paper keeps its iframe mounted (only the active one visible) so tab-switching / re-opening is instant (no re-parse / MathJax re-render). **(6) Session-swap** closes the panel (it shows the active session's references). **(7) Stale-chunk 404** — because `paperhub-reingest` re-creates `chunks` with fresh AUTOINCREMENT ids (preserving `paper_content.id`), `[chunk:<id>]` markers in pre-reingest answers 404; the canvas shows an inline "passage no longer available — paper re-indexed" notice (NFR-02), distinct from a network error. **No DB schema change** (two new read-only endpoints; `chunks`/`paper_content` unchanged). **No LLM-visible contract change.** **(8) Same-origin content (Wave 5)** — the panel reads the document's DOM to highlight + theme it, which a cross-origin iframe (`src` → backend) forbids (`contentDocument` is null), and a cross-origin/sandboxed PDF iframe either blanks or downloads. So the canvas **fetches the content (CORS) and embeds it**: HTML via an iframe **`srcdoc`** (the doc inherits the app origin → DOM-readable, content always shows, MathJax runs, figures are data-URI inlined) and PDF via **`react-pdf`** (pdfjs) rendering pages inline from the fetched bytes (`{ data }`) — no iframe, no download. (An earlier Wave-4 attempt used a Vite proxy + relative iframe URL; superseded by the embed approach, which needs no proxy or dev-server restart for correctness.) **(9) Background prefetch + session-survival (Wave 4/5)** — the panel stays mounted for the whole session (collapsed + `inert` when closed) and fetches every enabled reference's document up front into a per-paper cache (`docByPaper`), so papers are ready before first open and survive open/close; HTML views stay mounted (hidden) for instant switching, PDF renders only when active. FR-03 + §III-5.1 updated. **Dependency:** `react-pdf` (pdfjs-dist) added to the frontend; lazy-loaded with the canvas chunk. **Known follow-up (out of Plan D scope):** stable chunk ids across reingest so historical citations survive (Plan C reingest concern); browse-mode default-paper can flip if the refs list refreshes mid-browse (Wave 3 follow-up #1); papers with non-inlined HTML assets (the renderer inlines raster figures as data URIs, so this is rare) would need a same-origin asset path. |
| **v2.13** | **2026-05-21** | **Citation Canvas (FR-03) resolution strategy pinned to click-time text-search; the char-offset scroll assumption is corrected.** Implemented in Plan D. Motivation: FR-03 as written ("smooth-scroll to the chunk's `char_start`, highlight `[char_start, char_end]`") silently assumed the rendered HTML's character positions line up with `chunks.char_start`/`char_end`. They only do for the `<pre>` fallback renders (pylatexenc + raw envelope), where the body is exactly the escaped extracted text. On the **primary pandoc path** the HTML is a fully transformed DOM (headings, paragraphs, inlined figures, MathJax), so a LaTeX-source char offset points nowhere in the rendered document. **Decision**: the Canvas resolves a `[chunk:<id>]` click by **content, not offset** — it fetches the chunk's stored `text` and **text-searches the live HTML DOM** (whitespace-normalized prefix match via a `TreeWalker`), builds a `Range`, `scrollIntoView`, and wraps a transient `<mark>`. This is robust across both render paths and survives reingest. **Backend**: one new read-only endpoint `GET /chunks/{id}` → `{id, paper_content_id, section, text}` (the existing `GET /papers/content/{id}/html` already serves the document). **Frontend**: (1) inline `[chunk:<id>]` markers render as **academic-style sequential superscripts** (¹ ² ³), numbered per-message in first-appearance order and **deduped** (same chunk reused = same ordinal); (2) the Canvas is a right-edge slide-out drawer hosting the document in a **same-origin `<iframe sandbox="allow-scripts allow-same-origin">`** (style isolation + MathJax execution + `contentDocument` access for the search — trust boundary is our own locally-ingested papers); (3) a pure `findAndHighlight(doc, needle)` utility does the locate-and-highlight; a failed search surfaces a "couldn't locate this passage" toast (NFR-02 no-silent-failure), not the richer section-jump (deliberately out of scope). The Canvas component is **lazy-loaded** via `React.lazy` + `Suspense`, closing Plan B known-follow-up #1. **No DB schema change** (chunks already carry `paper_content_id`/`section`/`text`); **no LLM-visible contract change** (same `[chunk:<id>]` markers in `final`). FR-03 + NFR-01 + §III-5.1 updated. Plan D document carries the per-task TDD breakdown. |
| **v2.12** | **2026-05-21** | **`paper_suggest` — a sibling topic-recommendation intent, distinct from `paper_search`'s resolve-a-named-paper job.** Motivation: "recommend a few papers on X" and "find me the paper titled Y" are different tasks the v2.7 four-stage pipeline conflated under one Parser. `paper_suggest` **reuses the entire `paper_search` pipeline** (Parser → Processor [Discover→Resolve] → Finalizer → Synthesizer) and swaps only two prompts: (1) a **Parser** that decomposes the topic into 2–4 **intersection-anchored** angles — each angle keeps the topic's domain (e.g. "flow matching *for discrete diffusion*", never bare "flow matching") so the Discoverer doesn't drift off-topic; (2) a recommendation-toned **Synthesizer**. The auto-attach Finalizer (cap 2/turn), the `web.*` discovery-only contract, the combined 3-call cap, the `search_results` + `final` SSE shapes, and the three-attach-paths model are all reused unchanged. The router classifies `paper_suggest` as a fifth active intent (alongside `paper_search`/`paper_qa`/`slides`/`library_stats`, `chitchat` fallback); `effective_query` dispatch (v2.11) carries the resolved topic. **No DB schema change; no new tool surface** — same palette, swapped prompts. FR-06 updated to enumerate `paper_suggest` as a fifth active intent; the shared four-stage subgraph it reuses is the one described in §III-3 Research Agent row + FR-07 (v2.7). (Revision-history row backfilled in v2.13 to reconcile the SRS with the already-shipped v2.12 code.) Plan C v2.12 section in the implementation plan has the per-task TDD breakdown. |
| **v2.11** | **2026-05-20** | **Router context dispatch: the history-aware router resolves the latest turn's anaphora into a self-contained task brief and dispatches *that* to every downstream agent; a new `clarify` intent replaces the accidental empty-results re-ask.** Motivation: live multi-turn testing (session 72) — turn 1 established "continuous-diffusion flow matching / shortcut models / distillation; do these exist for discrete diffusion / DLMs?", the assistant offered to search "Discrete Flow Matching / Discrete Diffusion Distillation", and turn 2 was the bare "推薦幾篇" (recommend a few). Reading the recorded run from SQLite (per the agent-flow tracing recipe) showed `router:classify` correctly classified `paper_search`, but `paper_search:parse` was handed only `{"user_message":"推薦幾篇"}` → returned `requests:[]` → finalize emitted 0 candidates → the synthesizer asked the user to name a topic. **Root cause**: the router reads `state["history"]` but the Parser (and every other downstream agent) was fed only the raw `state["user_message"]`; the topic lived in prior turns the Parser never saw. **Architecture**: `RoutingDecision` gains one field, `resolved_query` (default `""`) — the anaphora-free, self-contained rewrite of the latest message, produced by the router because it is the only stage already holding `history`. The router writes it to a new `AgentState` slot `effective_query` (`= decision.resolved_query or user_message`), and every downstream agent — paper_search Parser + Synthesizer, paper_qa per-paper subagent + finalizer, chitchat — reads it through one DRY helper `paperhub.agents.state.effective_query(state)` (fallback to raw `user_message`) instead of the bare turn. The raw user text is still recorded verbatim in the `messages` table (persisted *before* routing), so the transcript and the frontend-rebuilt `history` stay truthful — only the *agents* act on the resolved brief. A new `intent="clarify"` lets the router short-circuit the pipeline and surface its own clarifying question (chat.py `clarify` SSE branch + `build_graph` `_clarify` node, both falling back to a shared `CLARIFY_FALLBACK` when `resolved_query` is empty) instead of dead-ending in the synthesizer's degenerate "which topic?" re-ask. **Observability is free**: the router already runs `record_result(decision.model_dump())`, so `resolved_query` lands in the `tool_calls` row and `runs.routing_decision_json`. **No LLM-visible contract change** beyond the additive router field; **no DB schema change** (`effective_query` is in-memory graph state only). Verified with a real-LLM check: the production router model resolves the session-72 "推薦幾篇" to `resolved_query="recommend representative papers on flow matching, shortcut models, and distillation techniques for discrete diffusion models and diffusion language models"`, `intent=paper_search`. §III-3 Research Agent row + §III-7 updated. Plan C v2.11 section in the implementation plan has the per-task TDD breakdown. |
| **v2.10** | **2026-05-20** | **`paper_qa` rebuilt from dense-RAG map-reduce into an agentic hierarchical retrieval pipeline; full-flow trace observability added to both paper_qa and paper_search for harness eval.** Motivation: live MolmoACT2 + X-VLA testing returned "empty papers" answers despite both references enabled. Two root causes — (1) a chunker arithmetic bug (the shrink-loop heuristic `tentative_end -= (tok_len - hard) * 4` overshot on dense LaTeX, clamped to `cursor+1`, and emitted thousands of 1-character chunks: paper 15 had 29,804 ≤5-char chunks of 29,891 total), and (2) the map-reduce architecture's ceiling — the synthesizer only ever saw the per-paper analyst's *prose summary*, never the raw chunks, and dense top-k retrieval flattens the section structure academic papers carry. **New architecture (Plan C v2.10)**: `paper_qa` is now a four-node LangGraph subgraph `pq_resolve → {pq_empty | pq_dispatch} → pq_finalize`. `pq_dispatch` fans out one **per-paper subagent** per enabled paper via `asyncio.gather`; each subagent runs a bounded tool-calling loop over **two tools — `list_sections()` (free) + `read_section(name)` (counts against `MAX_SECTION_READS=5`)** — navigating the paper by its section table-of-contents rather than by cosine-similarity top-k, picking the chunks that answer the question and citing them with `[chunk:<id>]` markers. `pq_finalize` runs the flagship finalizer over the **raw cited chunk text** (NOT analyst prose — flagship+chunks beats flagship+summary-of-chunks for correctness), preserving `[chunk:<id>]` markers for the Citation Canvas. The empty-picks short-circuit returns a "checked every enabled reference but none relevant" message; the subagent's best-effort fallback hands every read chunk to the finalizer when the LLM bails without citing. **Schema**: one additive column `paper_content.sections_json TEXT` (nullable; section TOC computed at ingest, idempotent ALTER migration). **Chunker hardened**: safe-halving shrink (never overshoots below `cursor+1`), LaTeX `%`-comment strip with `\%`/`\\%` parity handling, paragraph-boundary-preferred close. **`paperhub-reingest` CLI** re-chunks + re-embeds existing `paper_content` rows under the v2.10 chunker (preserves `paper_content.id` so membership + history survive; routes through `extract_latex`/`extract_pdf`, NOT raw `source_path`; embed-before-delete so a model failure can't strand a paper). **Agent-flow observability policy** (now in CLAUDE.md, load-bearing): every agent step records enough state in its `tool_calls` row to reconstruct the full context from the DB alone (chunk IDs read + cited, section TOC, per-turn LLM content + tool calls, final summary) — applied to the paper_qa subagent AND retroactively to paper_search (parse records raw LLM content; synthesize records resolved paper_ids + not_found hints; new `paper_search:finalize` row records emitted candidates). Iron rule: no agent-flow fix without first reading the recorded run. **Models**: per-paper subagent uses a small current-gen model (`gemini-3.1-flash-lite` default via `PAPERHUB_PAPER_QA_SUBAGENT_MODEL`); finalizer stays flagship (`paper_qa_model`). ResearchDeps subagent fields are optional (`None` → fall back to `paper_qa_model` / module `MAX_SECTION_READS`), matching the `paper_search_parser_model` pattern. **No LLM-visible contract change** — same `[chunk:<id>]` markers, same SSE token/final shape. **UC-3 + FR-07 + §III-3 Research Agent row + §III-5.2 updated.** **Known follow-up (out of scope)**: PDF-uploaded papers (`kind='pdf_upload'`) have empty `sections_json` because the chunker only detects `\section{...}` — the subagent's `list_sections` returns `[]` and the LLM can't navigate; needs PyMuPDF heading detection or a subagent "read-all-chunks" fallback. Plan C v2.10 section in the implementation plan has the per-task TDD breakdown. |
| **v2.9** | **2026-05-20** | **Composer "Attach paper" finally wired: PDF upload via new multipart `POST /papers/upload` + arXiv-ID manual entry via the existing JSON `POST /papers`.** Closes the long-standing UI gap from the Plan B Composer ship, where the paperclip icon shipped as a disabled placeholder tooltipped "Coming in Plan C" because the agent path was first to land. **Backend**: new `POST /papers/upload` multipart endpoint (`session_id` form field + `file` UploadFile) accepts `application/pdf` only (415 on other MIME), enforces a `PAPERHUB_MAX_UPLOAD_MB` ceiling mid-stream (default 30 MiB, 413 on exceedance), streams to a `tempfile.mkdtemp()` directory with the client-supplied filename sandboxed via `Path(file.filename or "upload.pdf").name or "upload.pdf"`, then calls `PaperPipeline.ingest(IngestRequest(upload_path=..., upload_kind="pdf"))` directly. **Why bypass `add_paper_to_session_dispatch`**: that dispatcher is keyed off a `paper_id` *string* with `arxiv:` / `ss:` / `library:` prefixes — file bytes don't belong in the LLM-visible tool surface, and adding an `upload:` branch would have been a category error. The pipeline's existing `_ingest_upload` path (sha256-keyed cache, same chunk → embed → render flow as arxiv, persists with `kind="pdf_upload"`) was already production-ready; only the HTTP edge was missing. **Frontend**: `parseArxivId(input)` normaliser accepts bare new-style (`2310.06825`), bare old-style (`cs.AI/0701001`), `arxiv:` prefix (case-insensitive), full `arxiv.org/abs/...` and `arxiv.org/pdf/....pdf` URLs (including those with query strings like `?context=cs.LG` or `#section` fragments — a real-world copy-paste path from arXiv's own Share button), and tolerates trailing `vN` / `VN` version suffixes. Returns canonical `arxiv:<id>` or `null`. `uploadPdf(sessionId, file)` is a thin multipart client that deliberately bypasses the JSON `apiFetch` wrapper because `Content-Type` must be left unset for the browser to write the multipart boundary. The new `AttachPaperMenu` component replaces the disabled paperclip with a `@base-ui/react` Popover containing two Tabs (Upload PDF / Paste arXiv ID); it reuses the existing `appendReferenceLocal` store action so references land in `referencesBySession[backend_session_id]` immediately, and the Reference Sources panel refreshes without a roundtrip. **Race hardening**: both handlers snapshot `backend_session_id` at network-call start and bail with an info toast if the user switched sessions before the response resolved (without the snapshot, the success path would have written the new reference into the wrong session's bucket — a real hazard on slow uploads). **Tooltip parity**: trigger wrapped in the project's `Tooltip` via nested base-ui `render={...}` slots so the four icons in the Composer tool row look consistent (the three remaining Plan D/F/G placeholders stay disabled with their "Coming in …" tooltips). **arXiv failures surface twice**: as a sonner toast (transient) AND as an inline `role="alert"` block under the input (persistent, so the user has context after the toast dismisses). **No schema change, no LLM-visible contract change, no MCP surface change**: this is pure transport + UI. **Tests**: 4 backend (`test_papers_upload.py` — happy path, 415, 413 with `monkeypatch.setenv`, cache-hit on re-upload, plus 2 path-traversal regressions), 13 parser cases (`api.test.ts::parseArxivId`), 9 component cases (`AttachPaperMenu.test.tsx` — both happy paths, both error paths, cache-hit toast wording, missing-backend-session hint, invalid-arxiv inline rejection without network call, session-switch-mid-upload bail). **Deferred (out of scope by design)**: LaTeX-tarball upload (the `_ingest_upload(upload_kind="latex")` branch exists in the pipeline but the UX shape — one .tex file vs project tarball, how to surface multi-file errors — wants a separate brainstorm); drag-and-drop onto the chat thread; arXiv-ID autocomplete (that's agent territory). Plan C v2.9 section in the implementation plan covers the per-task TDD breakdown. |
| **v2.8** | **2026-05-19** | **Embedder + reranker pulled out of the uvicorn worker into a separate `paperhub-modelserver` process so model weights survive `--reload` cycles.** Closes the v2.7 forward-looking note. **Motivation**: live testing showed the embedder (SentenceTransformer ~110 MB) and reranker (CrossEncoder ~80 MB) as module-level singletons inside the FastAPI worker meant every `uvicorn --reload` (triggered by any save under `src/paperhub/`) re-imported the module graph and reset both singletons. The next ingest paid the full ~5-10s reload tax, AND if another reload fired mid-load the worker died with the in-flight arxiv download still streaming — observed in MolmoACT2 testing as a 10-minute hang followed by a cryptic `RemoteProtocolError`. **Shape**: new `paperhub.modelserver` package with three pieces. (1) `server.py` — small FastAPI app exposing `GET /health`, `POST /embed {texts}` → `{vectors}`, `POST /rerank {query, texts, top_k}` → `{indices, scores}`. Models lazy-load on first non-empty call; empty inputs short-circuit without touching the model (cheap pre-warm probes). (2) `__main__.py` — `python -m paperhub.modelserver` (and `paperhub-modelserver` script entry) reads `PAPERHUB_MODEL_SERVER_HOST/PORT` and launches uvicorn against the FastAPI app with `reload=False, workers=1`. (3) `spawn.py` — `ensure_running(host, port)` TCP-probes `/health` first; if reachable, returns `None` (reuse path — what makes `--reload` zero-cost: the previous worker's spawn outlives the reload, the new worker probes /health, sees green, skips spawn). If not reachable, spawns ONE detached subprocess (Windows `CREATE_NEW_PROCESS_GROUP` / Unix `start_new_session=True`) with `stdout=DEVNULL` so uvicorn's reload-kill of the worker's process group doesn't cascade. The lifespan does NOT track the proc on `app.state` and does NOT terminate at shutdown — that intentional leak is what keeps weights resident across reloads. Operators clean up via OS reboot or `taskkill /im python.exe` / `pkill -f paperhub-modelserver`. **Pipelines updated to thin HTTP clients**: `pipelines/embedder.py` keeps `_SentenceTransformersEmbedder` for in-process mode and adds `_HttpEmbedder` (httpx.Client over `/embed`); `rag/reranker.py` mirrors the pattern. Factory dispatches on `Settings.inprocess_models` (env `PAPERHUB_INPROCESS_MODELS=1` to opt back into in-process — used by tests and low-resource hosts). **Pre-warm is fire-and-forget**: `app.state.prewarm_task` is an `asyncio.Task` running embed + rerank via `asyncio.to_thread`. With cold HF cache the first `/embed` triggers a multi-minute SentenceTransformer download from HF Hub; under the old blocking pre-warm this hung the backend "startup complete" for the whole window. Now the worker becomes responsive immediately; warm-up completes in the background. Cancelled cleanly at shutdown. **Operator workflow**: bare `uv run uvicorn paperhub.app:app --reload --reload-dir src` auto-spawns + reuses the modelserver via the lifespan check; optional `scripts/start.ps1` orchestrates both processes for operators who want explicit lifecycle + visible modelserver logs. **No SRS contract change** at the LLM-visible layer; **no DB schema change**. Tests run with `PAPERHUB_INPROCESS_MODELS=1` set by conftest at module-import time so no test ever dials a non-running modelserver. New `test_modelserver.py` covers the server's wire contract (stub models via fixtures), HTTP-client serialisation (via `httpx.MockTransport`), and factory dispatch. **The "Plan I — Inference Server Extraction" working name in v2.7's forward-looking note is folded into this revision** — kept in-tree as a Plan C cleanup rather than spinning up a separate plan, because the actual surface area (one new package, two new tool-using files, no new endpoints, no schema change) was much smaller than the original speculation. §III-5.1 (Paper Pipeline) + §III-5.2 (RAG retrieval) + §III-6 (External Systems) updated. Plan C document gains a v2.8 cleanup-pass section. |
| **v2.7** | **2026-05-19** | **`paper_search` agent decomposed from a single 5-tool mega-prompt into four single-responsibility stages wired by LangGraph; operational hardening round captures device auto-detect + opt-in CUDA wheels + arxiv-ingest resilience + MCP registry resilience + Windows event-loop fix.** Architectural motivation: the v2.6 single-LLM-turn paper_search agent (5 tools — `papers.search_library` / `papers.search_semantic_scholar` / `papers.find_related_papers` / `web.search` / `web.fetch` — plus ~200 lines of HARD-REQUIREMENT prompt blocks for multi-paper fan-out + json:candidates emission + corrective retry + honest-stop reasoning) had the predictable failure mode in live UI testing: each new rule diluted attention on the previous ones, Semantic Scholar got hammered with re-queries, and the json:candidates block was silently dropped under load. v2.7 splits the agent into four focused stages with disjoint tool palettes: **(1) Parser** (`ps_parse`) — one small-model LLM call that splits the user message into N distinct `ParsedRequest`s; deterministic arxiv-ID + DOI pre-scan short-circuits common cases before the LLM is consulted. **(2) Processor** (`ps_process`) — fan-out per `ParsedRequest` via `asyncio.gather`; each branch runs a bounded **Discover→Resolve inner loop** (kick-back on NotFound, capped at `MAX_REFINEMENT_LOOPS=1` — production tracing showed the kick-back loop just churns near-duplicate titles with tweaked years). `web.search` is the **Discoverer's only tool**; `papers.search_semantic_scholar` is called **exactly once per request** by the Resolver — that property is now architectural, not a prompt rule the LLM can ignore. **(3) Finalizer** (`ps_finalize`) — emits `SearchResultsYield` deterministically from the resolved set in Python (the LLM never authors `json:candidates`; Python builds `SearchCandidate`s from `ResolvedPaper`s, so block emission is structurally guaranteed). **(4) Synthesizer** — short prose summary only; small-model LLM call constrained to a ~30-line prompt. Each stage carries its own ~30-line prompt; tool-step streaming is preserved (each node drains `drain_tool_calls_since` between stages so the frontend trace panel updates per-stage rather than batched at end). Discoverer-side mitigations land in the same revision: forbid quoted single-token hints; **structured-output `paperhub.search_web(paper_hint, extra_terms)` wrapper** that hides raw `web.search` from the LLM so quoting (which empirically kills DuckDuckGo recall: `"MolmoACT2"` returns 0 hits, bare `MolmoACT2` returns 10 incl. arxiv URLs) is structurally impossible at the JSON-schema layer; exclude `bing` from open-websearch's auto-spawn engine pool (it returns 401s in the no-key path); extract `arxiv_id` from web hits and synthesise a `ResolvedPaper` when SS misses but the Discoverer surfaced an arxiv URL. **No SRS contract change**: the `search_results` + `final` SSE shapes, the read-only FR-07 loop semantics, the 3-call combined cap on `papers.search_semantic_scholar` + `web.*`, and the three-attach-paths model (agent-finalize auto-attach max 2 + user Add card + Library Browser) all preserved. **No DB schema change.** **Operational hardening packaged in the same revision**: (a) **opt-in CUDA torch via uv extras** — `pyproject.toml` exposes `cu124` / `cu126` / `cu130` extras; default install stays CPU-only so a clean clone doesn't pull 2 GB of CUDA wheels. (b) **Device auto-detect** — new `paperhub.pipelines._device.resolve_device()` walks CUDA → MPS → CPU, distinguishes *CPU-only-wheel* from *CUDA-wheel-no-GPU* in the warning text, honours `PAPERHUB_DEVICE=cpu|cuda|cuda:1|mps` override; SentenceTransformer + CrossEncoder singletons now pass `device=` explicitly so GPU operators stop getting silent CPU inference. (c) **arxiv ingest hardening** — `Range:` resume on transient drops + PDF fallback when the source archive trips arxiv's per-connection size cap (the daemon currently 503s with no body; we detect the size-cap signature and fail fast into the PDF path rather than retrying the source); tarball subdirs preserved; missing `\input` targets logged not silently elided. (d) **MCP registry cooldown** — failed-server connects retry after a 30-second cooldown rather than being dead-for-the-process-lifetime (so an operator who starts `open-websearch serve` mid-session sees `web.*` enter the palette without a backend restart); IPv4 probe fallback when `::1` resolution fails on Windows hosts; registry-connect path serialised under an `asyncio.Lock` to fix an `asyncio.gather` race during lazy-connect. (e) **Windows Proactor event loop** — `app.py` + MCP subprocess spawn force `WindowsProactorEventLoopPolicy` so MCP stdio servers (open-websearch's npm subprocess flavour) auto-spawn instead of failing on the Selector loop's no-subprocess-support. (f) **Frontend sync** — `search_results` SSE event auto-adds resolved papers to the active session and triggers a Reference Sources panel refresh, eliminating the v2.6-era "I see candidates but the drawer is empty" gap; graceful Add-button disable when backend `session_id` is still null. **Forward-looking note (in-flight on this branch, NOT scoped to Plan C as shipped):** the embedder + cross-encoder reranker are being pulled out of the backend (uvicorn) process into a **separate `paperhub.modelserver` process** so the model weights survive `--reload` cycles and the backend's deps drop torch + sentence-transformers + cross-encoder. Shape under construction (visible in the working copy at the time of this revision): thin `httpx.Client`-backed `_HttpEmbedder` / `_HttpReranker` implementations behind the existing `Embedder` / `Reranker` Protocols, selected at factory time via `PAPERHUB_INPROCESS_MODELS=0|1` (default 0 → remote model server; tests + low-resource hosts set 1 to load in-process). The v2.7 lazy-singleton-with-`device=` shape is the seam — minimal blast radius into call sites. Being scoped as a separate plan (working name: **Plan I — Inference Server Extraction**); the partial commits live on the same branch as the v2.7 decomposition but the full surface (server spawn lifecycle, health probe, batch packing, smoke scripts, mypy/test parity, documented operator workflow) lands separately. Will surface as a v2.8 SRS revision once that plan closes. |
| **v2.6** | **2026-05-19** | **`paperhub-papers` FastMCP server pulled forward from Plan E into Plan C v2.5 scope; the Research Agent's own three tools now route through MCP (HTTP loopback), completing the SRS-original "every tool via MCP, no manual tools" architecture.** v2.5 (below) shipped the MCP *client* layer with open-webSearch as the first integrated server and explicitly deferred retroactive MCP-wrapping of `search_library` / `search_semantic_scholar` / `find_related_papers` to Plan E. v2.6 reverses that deferral — keeping the dual dispatch path (in-process `papers.*`, MCP-client `web.*`) re-introduces the exact "manual tool" pattern that motivated the v2.5 course-correction, just dressed up. **Architecture**: a `paperhub-papers` FastMCP server is mounted **in-process as a sub-app on the existing FastAPI app at `/mcp`** (no extra subprocess, no second port). The Research Agent's `MCPClient` connects to `http://localhost:<backend_port>/mcp` over loopback — uniform routing for every tool the agent has. The same `/mcp` surface is reachable by external MCP clients (Claude Desktop, Cursor — SRS §III-6 last paragraph's promise), so this delivers the `paperhub.*` MCP surface at the same time. **Bootstrap**: the MCP registry connects lazily on first tool use (not at FastAPI startup) so the `papers` server entry pointing at the backend's own port doesn't race with uvicorn's bind. **Tool surface**: `papers.search_library` / `papers.search_semantic_scholar` / `papers.find_related_papers` (namespace prefix `papers.`, same JSON-schemas as the in-process versions today — zero contract change at the LLM-visible layer). The current `add_paper_to_session_dispatch` Python callable **stays in-process** — it's not in the LLM-visible palette (v2.4) and is called by FastAPI endpoints, not by the agent. **§III-6 + §III-6.1 updated**: External Systems table gains a `paperhub-papers` MCP row; the "deliberate scope cut" paragraph at the bottom of §III-6.1 is **rescinded** (struck through with a note pointing here). **Plan C v2.5 follow-ups updated**: Task v2.5-3 is inserted (build the FastMCP server + mount on FastAPI), the existing v2.5-3 (Research Agent integration) renumbers to v2.5-4 and is broadened to remove the in-process dispatch path entirely, downstream tasks renumber. No FR/UC/Acceptance-criteria changes; no DB schema change. Plan E now only owns the sqlite MCP server (one fewer Plan E concern). |
| **v2.5** | **2026-05-19** | **MCP client layer ships in Plan C as course-correction; `Aas-ee/open-webSearch` is the first integrated external MCP server, unblocking the weak paper-search UX on short / vague queries.** Motivation: the v2.4 Research Agent's free-text `search_semantic_scholar` consistently misses on indirect references — "that diffusion paper everyone cites", "the Mamba follow-up", "RAG 的原始論文" — and the agent then burns its 3-call external-search budget on slight rephrasings. A no-key general-web *discovery* step (DuckDuckGo / Bing / Brave / Startpage via open-webSearch's multi-engine surface) lets the agent lift canonical paper titles from blog posts, "papers I love" lists, and GitHub READMEs *before* refining into `search_semantic_scholar` for citable hits. **Course-correction note**: §III-6 v2.2 said "no `web_search` MCP" because no organic need was projected; the v2.4 paper-search UX shows otherwise. §III-7 v2.2 also expected Plan C to ship a `paperhub-papers` MCP server but Plan C shipped manual in-process dispatchers instead — retroactive wrapping of those existing tools (`search_library`, `search_semantic_scholar`, `find_related_papers`) is **accepted as technical debt** and deferred to Plan E alongside the sqlite MCP server. The *client* infrastructure (process-wide `MCPRegistry`, namespaced tool aggregation, streamable-HTTP transport via the official `mcp` Python SDK) lands now in Plan C so Plan E (sqlite) + Plan G (filesystem, paperhub.*) become **pure config additions** (one `[[server]]` block in `mcp_servers.toml`), not new plumbing. **Wire change**: new tools `web.search(query, limit, engines)` and `web.fetch(url, maxChars)` enter the Research Agent palette under the `web.*` namespace, **discovery-only** — results never carry a `paper_id`, never enter `recent_results`, and cannot appear in the `json:candidates` shortlist; the agent must round-trip through `search_semantic_scholar` (or `search_library`) for any citable pick. The existing `MAX_EXTERNAL_SEARCH_CALLS_PER_TURN = 3` cap is broadened to cover `search_semantic_scholar` + `web.search` + `web.fetch` **combined**, so the agent cannot burn the budget on web alone. **Conditional**: tools only enter the palette when the registry has a reachable `web` server; if `open-websearch serve` is not running, the registry has no `web` entry, the tools don't surface, and `paper_search/v1` loads instead of the new `paper_search/v2` prompt — behaviour reverts to v2.4 exactly, no errors surfaced to the user. **Operator workflow**: open-webSearch is an *optional external dependency*, same posture as `pandoc` — `npm install -g open-websearch`; run `open-websearch serve` in a separate shell; backend probes the daemon at startup and degrades gracefully if it's down. **Architecture additions**: §III-6 gains an `open-webSearch` row and an "MCP client layer" subsection (`paperhub.mcp.client` + `paperhub.mcp.registry` — reusable connector that all three planned MCP integrations consume); Part 0 MCP-demos row updated to reflect `web_search` going from "omitted" to "integrated"; FR-07 + §III-3 Research Agent row gain a "discovery → refine" canonical-flow paragraph. No schema change. No change to `search_semantic_scholar`, `find_related_papers`, `search_library`, or `add_paper_to_session_dispatch`. |
| **v2.4** | **2026-05-18** | **Semantic Scholar promoted to primary external search; arXiv reserved for raw-source download. Agent shortlists candidates with optional finalize markers (1-2 max per turn) for auto-attach; remaining picks are user-driven.** Two coupled shifts: (1) **Tool palette**: `search_arxiv` is removed from the LLM-visible palette and replaced by `search_semantic_scholar(query, max_results)` against `/graph/v1/paper/search` — broader coverage (~200M papers vs arXiv subset) and richer metadata (citation counts, openAccessPdf URLs, externalIds). `find_related_papers` (Semantic Scholar citation graph) is unchanged. **`add_paper_to_session` is removed from the LLM-visible palette entirely** — the agent never calls a write tool. Instead, the agent emits a structured shortlist (3-5 candidates) where each pick has an optional `finalize: true` marker (capped at 2 per turn server-side regardless of what the LLM emits). The chat endpoint **auto-attaches finalize-flagged candidates** via `add_paper_to_session_dispatch` before emitting the `search_results` SSE event; non-finalized candidates render as cards awaiting the user's Add click. Three sources of an attached paper therefore exist: (a) **agent-finalized** (the 1-2 most-confident picks per turn auto-attach), (b) **user adds from SearchResultList card** (suggested-but-not-finalized candidate the user wants), (c) **user adds from Library Browser** (previously-indexed paper from any prior session). Research surfaces long candidate lists; the finalize cap + user-curated tail prevents wasteful downloads while still giving the agent a confident default. (2) **Ingestion entry points**: chat endpoint dispatch (for finalize-flagged picks) + `POST /papers` (for user clicks on SearchResultList cards) + `POST /papers/from-library` (for Library Browser attaches). All three paths go through `add_paper_to_session_dispatch(paper_id)` so cache semantics + canonical `content_key` are identical regardless of source. The dispatcher picks the download path — arXiv source preferred (LaTeX → highest-fidelity chunking), else `openAccessPdf.url` PDF via PyMuPDF (`kind='pdf_upload'`, `sha256` content_key), else raises `NoIngestibleSourceError` (HTTP 422 on POST /papers; chat endpoint marks the candidate as `auto_added=false, error="no_ingestible_source"` and surfaces a greyed-out Add button). **Wire change**: a new SSE event `search_results` carries the candidate list ({paper_id, title, authors, year, abstract, arxiv_id, has_open_pdf, reason, finalize: bool, auto_added: bool, papers_id: int \| null, error: str \| null}) emitted just before `final` on every paper_search turn. Frontend renders the list as a `SearchResultList` below the assistant message; auto-added rows show an "Added by agent ✓" badge with the same toggle/remove controls as user-added refs; remaining rows show Add buttons. UC-1 + UC-2 + FR-07 + FR-08 + §III-3 Research Agent row updated. No schema change — `paper_content.kind` already allowed `'pdf_upload'` in v2.2, `POST /papers` adds the `paper_id` field. Plan C ships the agent code unchanged at the contract level; the operational gaps (frontend session_id roundtrip, streaming `tool_step` events during the paper_search loop, TraceInline arg/result expand-on-click, Reference Sources panel UI, SearchResultList UI, SS-primary palette, finalize-marker behavior) land as Plan C v2.4 follow-ups. |
| **v2.3** | **2026-05-18** | **Research Agent becomes a curating, library-aware tool-calling loop.** UC-1 + UC-2 + FR-07 + FR-08 + §III-3 Research Agent row updated: `paper_search` is no longer a single arXiv call. Mission is now to **extend the session's reference set on the user's behalf**, informed by what's already in it (follow-up paper discovery). Three tools — `search_library` (read indexed paper_content), `search_arxiv` (read external), `add_paper_to_session` (write, cache-aware; inserts with `enabled=true` so additions are immediately in scope for the next `paper_qa`). Per turn the agent may ask a clarifying question, search library and/or arXiv (arXiv capped at N=3 calls), and **default-add the top 1-2 results** so the user gets immediately usable references. Three layers of deduplication prevent duplicate work: prompt context block of current references, `search_library` excluding already-attached papers, DB-level `UNIQUE` constraints. Cross-turn refinement falls out via `AgentState.history`. User retains full deterministic UI control via PATCH `/papers/{id}` (toggle), DELETE `/papers/{id}` (remove), POST `/papers/from-library` (manual attach) — agent never invokes these. New endpoints `GET /papers/library` + `POST /papers/from-library` let the UI surface manual library attachment. No schema change. |
| **v2.2** | **2026-05-17** | **Two-layer content/membership split.** Introduces `paper_content` (physical: one row per unique paper, owns `chunks` + Chroma vectors + on-disk artefacts) and reshapes `papers` into a thin session-membership layer (`session_id`, `paper_content_id`, `enabled`, `added_at`). Adds a shared on-disk cache `workspace/papers_cache/{arxiv/<arxiv_id>,upload/<sha256>}/`; re-importing a paper into a new session skips download, extraction, HTML render, chunking, and embedding. Cache GC policy: **never** for the demo (monotonic growth). FR-08 rewritten around the two layers; §III-5.1 Paper Pipeline gains a cache-lookup first stage; §III-5.3 Slide Pipeline gains an explicit figure-path resolution rule so `\includegraphics` and Markdown image references continue to resolve after sources moved out of the per-session tree. Schema: 6 → 7 tables. |
| **v2.1** | **2026-05-17** | Stale-v1 cleanup. Removed dangling "delegated to `paper2slides-plus`" phrasing from §I-7, §II-3, NFR-03, and Acceptance I-8 #6. Resolved Compare-view audit model: a Compare turn is **one `run_id`** with a `branch` discriminator on `tool_calls` (FR-04, FR-09, §III-7). Citation Canvas source resolved: full paper is **pre-rendered to HTML at import time** by the Paper Pipeline, written to `papers.html_path` (§III-5.1, §III-7, FR-03). Slide-rendering framework remains explicitly deferred — flagged in §III-5.3. Editorial fixes: `session_references` join-table residue in UC-2 / §III-2, "7 tables" / "6 tables" mismatch in §III-1, broken `§III-6` cross-references in §III-3 (correct ref is §III-5.3). |
| **v2.0** | **2026-05-17** | **Reset + decomposition.** Restructured into the standard three-part SRS template (需求規格書 / 技術選型分析 / 系統架構設計). PaperHub owns its data layer, RAG knowledge base, and slide pipeline so every retrieved chunk and every generation step is traceable. `paper2slides-plus` is **decomposed**: useful extraction / LaTeX utilities are copied into the repo and adapted; its chat-with-paper and slide-pipeline orchestrators are NOT reused (they can't expose chunk-level citations for the Citation Canvas, and the slide pipeline needs a modern framework rewrite). `arxiv` MCP is replaced by the copied arXiv-download utility. The only artefact carried from v1.x is the data schema (§III-7). |

---

# Part 0 — Requirement Coverage Matrix

A top-level cross-reference from each high-level project requirement to the FR / NFR / §-number that addresses it. Keeps coverage explicit and surfaces any silent gap during review.

| Requirement | Addressed in |
| --- | --- |
| Multi-model + tool-routing AI platform | FR-06 (Router, 4 intents), FR-04 (multi-model Compare view), §III-4 (LLM Module, tiered models) |
| Single demo integrating paper Q&A + NL2SQL + report generation + MCP tools | UC-1..5 cover all four; FR-05..08 implement them; §III-3 wires them into one LangGraph |
| Visible *"how the agent picks tools"* | FR-01 RoutingBadge surfaces the picked intent + model_tier + reasoning per turn |
| Problem classification | FR-06 Router; acceptance I-8 #1 (≥ 80 % intent accuracy on 16-prompt fixture) |
| Tool selection logic | FR-01 RoutingBadge + FR-02 TraceInline + acceptance I-8 #1 |
| Model answer-quality comparison | FR-04 Compare view + acceptance I-8 #5 (hand-evaluated 10×2 table) |
| Traceable tool-call records | FR-02 TraceInline + FR-09 audit-log writer + acceptance I-8 #4 (replay from SQLite) |
| Frontend: Streamlit *or* React | **React** chosen (Vite + Tailwind + Zustand) — see §III-2; rationale: the Citation Canvas UX is component-shaped, harder to ship in Streamlit |
| Backend: FastAPI + LangGraph *or* Google ADK | **LangGraph** chosen — see §III-3; rationale: larger MCP-adjacent ecosystem; the node-graph metaphor makes the tool-selection logic visually unambiguous. Google ADK is a drop-in alternative behind the `LlmAdapter` Protocol |
| All four agents present: Router, Research, SQL, Report | §III-3 names all four as discrete LangGraph nodes; FR-06 (Router), FR-07 (delegation contract for Research / Report), FR-05 / FR-07 (SQL Agent) |
| Data layer: SQLite *or* DuckDB | **SQLite** chosen (§III-7, 8 tables). DuckDB was evaluated as an opt-in OLAP path but dropped in v2.16 — the demo fixture set is small enough that plain SQLite handles every `library_stats` query, so the second engine was dead scope |
| Paper data via arXiv API or preprocessed | The Paper Pipeline (§III-5) reuses copied utilities from `paper2slides-plus` for **all three input shapes** — arXiv ID (via `arxiv` Python client), raw PDF, and raw LaTeX project archives. No external `arxiv` MCP server is involved |
| MCP demos: filesystem, SQLite, web search, calendar, or custom | `filesystem`, `sqlite`, a **custom `paperhub.*` server**, and **`open-webSearch`** (no-key multi-engine general web search) are wired (§III-6). v2.5 promotes `web_search` from "omitted" to "integrated" — the v2.4 paper-search UX showed weak recall on short / vague queries, and `Aas-ee/open-webSearch` (DuckDuckGo / Bing / Brave / Startpage / Baidu / Sogou behind one MCP surface) gives the Research Agent a no-key discovery step before the existing Semantic Scholar refine call. Calendar MCP remains omitted — still no organic use case. **v2.16** ships the `sqlite` MCP as an in-process read-only `sql` FastMCP server (the SQL Agent's NL2SQL backend) plus a sibling write-capable `memory` FastMCP server (session+global memory) — both custom in-repo servers, fulfilling the `sqlite` + custom-`paperhub.*` categories. The shipped tools span the local-FS, local-DB, external-web, and custom-server categories implied by the listed options |
| Emphasis on tool description + safety boundary | NFR-05 (scope declarations per tool); §III-6 lists each scope explicitly; FR-02 surfaces `rejected` rows in yellow |
| Evaluation: right tool? source cited? SQL executable? | Acceptance I-8 #1 (tool routing), #3 (citations from ≥ 2 papers), #4 (NL2SQL ≥ 70 % first-try executable); model-comparison sweep emits a markdown report |
| Persistent agent memory (session + global) *(v2.16)* | UC-7 (remember / recall / edit / forget); FR-10 (Memory store, incl. v2.17 governance: gate + status lifecycle + conflict-supersede + active-only recall); §III-6 `memory` MCP row (write surface, scope-enforced). Router `memory` intent (FR-06) + autonomous `memory.add`/`memory.recall` on `paper_qa` / `library_stats`; recall injected by default |
| Memory Manager UI + governance *(v2.17)* | FR-11 (Memory Manager panel + REST endpoints); §III-2 (MemoryManager component); §III-6 `memory` MCP row + §III-7 `memories` DDL (status/supersedes/superseded_by columns); UC-7 extended |

When the project's requirements evolve, new items get a row in this table and a corresponding FR / NFR / acceptance entry — never silently absorbed.

---

# Part 1 — 需求規格書 (Software Requirements Specification)

## I-1. 專案背景 (Project Background)

Two reference projects exist in the author's portfolio that PaperHub draws from:

- **`paper2slides-plus`** — a FastAPI service that turns arXiv IDs / PDFs / LaTeX-zips into Beamer slide decks, with a chat-with-paper Q&A mode. **Decomposed, not run as a service.** Its extraction utilities (arXiv source download, PDF text extraction, LaTeX project handling) are copied into PaperHub and adapted; its chat-with-paper module is *not* reused because it cannot surface chunk-level citation IDs for the Citation Canvas; its slide-pipeline orchestrator is *not* reused because a modern-framework rewrite is needed (§III-6).
- **`Intro2GenAI-hw1`** — a chat UI (Node + Express + vanilla JS) that streams via SSE, manages history, forks conversations, renders thinking blocks, and integrates MCP tools. Its UX idioms are ported to React; its Node backend is not deployed.

PaperHub is therefore a self-contained system: a chat surface, a router, four agents, an in-repo paper knowledge base with RAG, an in-repo slide pipeline, and a tool-call audit log. The four product dimensions PaperHub commits to are problem classification, tool selection, model answer-quality comparison, and traceability — see §I-5 (functional requirements) and §I-8 (acceptance criteria) for the concrete shape of each.

**Engineering principle.** Invest in UX; reuse code by *copying and adapting*, not by service integration. The chat shell, citation canvas, routing badge, trace panel, and LangGraph router are new code; the paper-extraction utilities are copied + edited from `paper2slides-plus`; the chat-shell layout idioms are copied + ported from `Intro2GenAI-hw1`. Every step of the pipeline is in-repo so every trace row points at code we control.

## I-2. 問題描述 (Problem Description)

Today's chat interfaces to LLMs hide three things this product makes first-class:

1. **Opaque tool / model selection.** A general-purpose chat can answer a paper question, run an SQL aggregation, or call an external tool — but the user cannot tell whether the agent picked the right capability or whether a wrong answer was caused by the model, the prompt, or a wrong tool firing.
2. **Ungrounded citations.** When an LLM says *"according to §3.2"*, there is no path from the cited token back to the actual paragraph in the source paper. For research use this is fatal.
3. **Non-reproducible agent runs.** Without a structured per-step audit trail (model, tool, arguments, latency, status), multi-agent answers cannot be debugged or compared across model choices.

PaperHub addresses all three **at the UI surface**: a **routing badge** makes (1) visible per turn; a **citation canvas** with click-to-scroll-and-highlight makes (2) inspectable; a **trace panel** sourced from a `tool_calls` SQLite table makes (3) replayable. The backend doing the actual research work is reused from existing services — the contribution is the inspection layer on top.

## I-3. 利害關係人與使用者角色 (Stakeholders & User Roles)

| Role | Concern |
| --- | --- |
| Primary end user — researcher / technical reviewer | Polished demo flow; visible routing decisions; inspectable citations; auditable traces. |
| Presenter / operator | Reliable demo, reproducible from a single repo; cost-bounded LLM usage. |
| Reference projects | `paper2slides-plus` (extraction + LaTeX utilities, decomposed and copied into this repo); MCP servers (`filesystem`, `sqlite`, custom `paperhub.*`). |
| LLM providers (Gemini / Anthropic / Ollama) | API quotas; key redaction in audit log. |

## I-4. 使用情境 (Use Cases — 6)

The reading flow is intentionally split into three explicit moments: **search**, **curate**, **discuss / generate**. The user always sees a stable shortlist of "reference sources" attached to the current chat session, and can toggle individual ones on / off — every downstream Q&A or slide generation honours that toggle.

### UC-1 — Search arXiv and shortlist candidates
*"Find recent papers on Mixture-of-Experts routing."* → Router picks `paper_search` → Research Agent runs a **read-only tool-calling loop** (v2.4 — read-only palette, shortlist + optional finalize markers): per turn it decides whether to (a) ask a clarifying question when the user intent is too vague to search (e.g. *"find me good ML papers"* → *"What problem are you trying to solve — routing, training stability, or something else?"*), (b) call `search_library(query)` against the user's deduplicated indexed library, (c) call `search_semantic_scholar(query, max_results)` against `/graph/v1/paper/search` (v2.4 — was `search_arxiv` in v2.3), or (d) call `search_semantic_scholar` again with a refined query (capped at N=3 calls per turn) before shortlisting. The agent's final assistant message names a **shortlist of 3-5 best candidates** with a one-line reason per pick + an optional `finalize: true` marker on the most-confident 1-2 picks. The chat endpoint **auto-attaches finalize-flagged candidates** (cap 2 enforced server-side; calls `add_paper_to_session_dispatch` → triggers download or cache-hit), then emits a `search_results` SSE event with the structured candidate list including `finalize`/`auto_added`/`papers_id`/`error` fields. The frontend renders each as a card with title / abstract / authors / year: auto-added rows show "Added by agent ✓" with toggle/remove controls; suggested-only rows show an **Add as reference** button. **Suggested-only papers are not downloaded** — only finalized (agent-auto) + user-clicked (Add card) + Library-Browser-attached papers trigger ingestion; cache-hit if already indexed. Multi-turn refinement across user replies is supported by the same agent reading prior `AgentState.history`; the user can answer the clarifying question in the next chat turn and the agent picks up the refined intent.

### UC-2 — Promote a paper to a session reference (download-if-needed or add-from-library)
The user clicks **Add as reference** on one or more search results → the Paper Pipeline (§III-5.1) runs. The pipeline's **first step is a cache lookup** on `content_key` (e.g. `arxiv:2403.01234`): if a matching `paper_content` row already exists (from a previous session), the pipeline **skips** download, extraction, HTML render, chunking, and embedding entirely, and just inserts a new `papers (session_id, paper_content_id, enabled=true, added_at)` row pointing at the existing content. On cache miss, the pipeline downloads, extracts, renders HTML, chunks, embeds, persists a `paper_content` row + `chunks` + Chroma vectors **once**, then inserts the `papers` row. Either way the paper appears in the **Reference Sources** sidebar panel under the current chat session, marked enabled by default. The user can toggle any reference on / off; toggling does *not* delete anything, it only flips `papers.enabled` for that row.

The user can also bypass the search step entirely by browsing their **already-indexed library** (the union of `paper_content` rows across all of their prior sessions) and attaching any of those papers to the current session — this is the cache-hit path surfaced as a first-class action (v2.3). The backend exposes `GET /papers/library` (paginated list of `paper_content` rows not yet attached to the current session) and `POST /papers/from-library` (inserts a fresh `papers (session_id, paper_content_id, enabled=true, added_at)` row referencing an existing `paper_content_id`; idempotent on `UNIQUE (session_id, paper_content_id)`). Outcome is identical to UC-2's cache-hit path — no download, no re-extraction, no re-embedding.

**Ingestion source selection** (v2.4 — runs through `add_paper_to_session_dispatch(paper_id)` regardless of the trigger): three entry points share the dispatcher — (1) the chat endpoint auto-attaches finalize-flagged candidates from the agent's shortlist (max 2 per turn), (2) `POST /papers` (user clicks Add on a SearchResultList card), (3) `POST /papers/from-library` (user attaches from the Library Browser drawer; always cache-hit by definition). For `paper_id="ss:<paperId>"` the dispatcher fetches the paper's metadata and chooses the download path: (a) if `externalIds.ArXiv` is present, recurse into the arXiv-source ingestion path (LaTeX source is the highest-fidelity ingest — better chunking, math intact, figures resolvable); (b) else if `openAccessPdf.url` is present, fetch the PDF via httpx and run the PyMuPDF extraction path with `kind='pdf_upload'` and `sha256(<pdf-bytes>)` as the `content_key`; (c) else raise `NoIngestibleSourceError` — chat endpoint marks the candidate as `auto_added=false, error="no_ingestible_source"` so the UI can grey out the Add button; `POST /papers` returns HTTP 422 with the same fields. The arXiv-source path is **always preferred over the PDF path** when both are available because LaTeX preserves structure that PDF text extraction loses. The canonical `content_key` (`arxiv:<id>` if arXiv-backed, `sha256:<hex>` for PDF-only) means the same paper picked via any entry point cache-hits future invocations. **Suggested-only candidates (no finalize flag, no user click) are not downloaded** — they remain metadata in the SSE event payload only.

### UC-3 — Multi-paper Q&A with the Citation Canvas *(headline)*
*"How do these two papers differ on expert collapse?"* → Router picks `paper_qa` → Research Agent runs the **v2.10 agentic hierarchical pipeline** (§III-5.2) over the **currently-enabled references**: one per-paper subagent per reference navigates that paper's section table-of-contents (`list_sections` → `read_section`), picks the chunks bearing on the question, and cites them; a flagship finalizer reads the raw cited chunks across papers and generates an answer with inline `[chunk:<id>]` markers tied to real `chunks.id` rows → clicking one opens the **Citation Canvas** on the right, scrolls to the chunk, highlights it. Disabling a reference and re-running the same question dispatches a subagent only for the remaining enabled set.

### UC-4 — Slide generation, editing, and presentation from the enabled reference set
*"Make slides comparing these references."* → Router picks `slides` → Report Agent reads the same enabled set → runs the in-repo Slide Pipeline (§III-5.3): **structure planning** (one thematic cross-paper **synthesis/survey** outline) → **per-section LLM generation** (RAG-grounded, chunks pulled across the enabled papers) → assemble + **compile to a Beamer PDF** (v2.18: framework locked to Beamer-via-LangGraph for conference-ready output — lossless math, real figures, tikz) with a compile-fix retry loop → **speaker notes** (one per page) → returns a **deck chip** in the chat. A single enabled paper degrades to a clean paper-summary deck. The pipeline reuses LaTeX helpers copied from `paper2slides-plus` but is orchestrated by a traced LangGraph subgraph. Toggling a reference off before generation excludes it from the deck.

**v2.21 — generation produces slides only; notes + edits are decoupled, opt-in follow-ups.** *"Make slides comparing these references"* generates **slides without speaker notes**, and the finalize message **hints** the next moves: generate notes · edit a slide · edit the deck. The user can specify **length** (*"a 20-minute talk"*, *"15 slides"*; default 20 min ≈ 15 slides). Notes are authored only when asked (*"幫我加講稿"*), and their **language is independent of the slides'** — *"把講稿變成繁體中文"* re-languages only the notes (the deck stays in its original language) **without regenerating the slides** (the v2.21 bug fix).

The deck is **session-scoped and persistent** — the agent knows whether a working deck exists (`decks` table, one per session). A follow-up `slides` turn is classified by `sl_resolve` (the deck-command classifier) into `generate_notes` / `edit_notes` / `edit_slides` / `regenerate`; an `edit_slides` turn **resolves scope** to the **slide currently on screen** (`current_view_page`), a **named page**, or the **whole deck** — *"make this equation bigger"* while viewing slide 5 edits frame 5; *"add a limitations slide"* edits the deck. Edits are **diff-based** (rewrite only the targeted `deck_slides` frame/note; never full regen) and snapshot a **version** the user can revert to (history parity with `paper2slides-plus`).

The user opens the deck in a **Slides panel** (filmstrip + current slide via react-pdf + collapsible speaker note, FR-12). **Presentation mode** keeps the chat + panel mounted (adding presenter controls — timer, next-slide preview) and opens a **separate fullscreen, slide-only window** on the second screen; every page switch syncs to it via `BroadcastChannel`. Mid-talk, an audience question typed into the composer runs a normal `paper_qa` turn (Citation Canvas answers with citations) while the presentation window holds its page and the panel returns to where it was — the full *present → get asked → answer from the papers → resume* loop.

### UC-5 — Library-stats NL2SQL (read-and-act)
*"How many papers did I add this week? Which sessions reference paper X?"* → Router picks `library_stats` → SQL Agent introspects the schema via the read-only `sql` MCP (`sql.list_tables` → `sql.describe`), emits a `SELECT` via `sql.query` (verb + table allowlist enforced by `sqlglot`; covers `paper_content`, `papers`, `chunks`, `chat_sessions`, `messages`, `runs`, `tool_calls`), self-repairs once on error → answer in the user's language + the executed SQL shown as a code block. "Which sessions reference paper X" is answered by joining `papers` ⨝ `paper_content` on `paper_content_id` and filtering on `paper_content.arxiv_id` — `paper_content` is the unique paper identity, `papers` is the per-session membership row (§III-7). **Two-layer scoping (v2.17 fix):** "my library" / "all my papers" questions target `paper_content` (the deduplicated cross-session paper store); "papers in this chat" / "references in this session" questions join `papers ⨝ paper_content WHERE papers.session_id = :session_id`. The SQL planner is seeded with the real schemas of both tables (titles/abstracts/arxiv_id live in `paper_content`; `papers` carries only `session_id`, `paper_content_id`, `enabled`, `added_at`) so it does not hallucinate non-existent columns. **Read-and-act (v2.16):** when the ask is "find the papers I already have on flow matching" the agent shortlists matching `paper_content` rows as candidates (`paper_id="library:<id>"`), auto-attaches up to 2 (`finalize:true` → `add_paper_to_session_dispatch`, always a cache-hit), and emits the same `search_results` SSE event UC-1 uses — so the `SearchResultList` renders auto-added badges + Add buttons for the rest. The agent never writes to `paper_content`/`papers` directly; auto-attach + the user's Add click are the only mutation paths (FR-08).

### UC-7 — Remember, recall, and edit facts (session + global memory)
*"Remember that I'm comparing MoE-routing papers for a survey"* → Router picks `memory` → the memory node first runs the **Memory Gate**: the rule-based safety check accepts this benign content, so the LLM scope classifier runs → writes a `scope='session'` memory (project-scope: a fact tied to this chat/project context) bound to this chat. *"Always answer me in Traditional Chinese"* → gate accepts → `scope='global'` memory (user-scope: a cross-session personal preference). The two scope values embody the functional "user vs project" distinction: `session` == project scope, `global` == user scope. *"Remember to use sk-abc123 as my API key"* → gate **refuses** (sensitive: API key pattern) and returns a visible reason; the `tool_calls` row is `status='rejected'`; the memory is not stored — even though the user explicitly asked. *"Use FastAPI for the backend"* → on add, an LLM conflict-detection call compares against active same-scope memories; if "use Flask" is active, the new memory is saved with `supersedes=<old-id>` and the Flask memory flips to `status='superseded'` + `superseded_by=<new-id>`. On a later turn (even in a different chat for global memories), the `paper_qa` finalizer and the `library_stats` agent receive a **recalled-memory context block** (top-k by FTS over `memories.content WHERE status='active'`, on by default) so answers respect remembered preferences/context. Superseded memories are never injected. *"Update my note about the survey — it's now about expert collapse"* → the memory node recalls the matching memory and `memory.edit`s it (ownership-checked: only this session's memories or global ones are editable, else `rejected`). Agents may also **autonomously** persist a salient durable fact mid-turn via `memory.add` (capped ≤2/turn; gate always runs first). All memory ops live in the dedicated write-capable `memory` MCP (§III-6); the read-only `sql` MCP cannot see the `memories` table. **Memory Manager panel (v2.17, FR-11):** the user opens the Memory Manager from the Composer or sidebar to view all memories — grouped by scope, with active/superseded badges and supersede-chain links. Per-row controls allow editing content, deleting, or toggling active↔superseded (re-activating a superseded note or soft-deactivating an active one). These UI-driven mutations go through the REST endpoints (`PATCH /memories/{id}`, `DELETE /memories/{id}`) — not through the `memory` MCP — because curation is a deterministic human gesture, not an agent action.

### UC-6 — Compare two models on the same question
Presenter toggles **Compare** in the composer, picks two models (e.g. `gemini-2.5-flash` vs `gemini-2.5-pro`), sends the same prompt → two answers stream side-by-side, each with its own routing badge + trace panel. The same enabled-reference set scopes both runs.

## I-5. 功能需求 (Functional Requirements — 9)

| ID | Name | Description |
| --- | --- | --- |
| **FR-01** | Routing badge | Every assistant message header shows a chip with `intent · model_tier · confidence%`. Click expands the Router's `reasoning` text. Sourced from `runs.routing_decision_json`. |
| **FR-02** | Trace panel | Per-turn collapsible panel listing each `tool_calls` row: `[agent] tool · status · latency`. Click a row to expand `args_redacted` + `result_summary`. Rejections render yellow; errors red. |
| **FR-03** | Citation Canvas | A right-side **reference reading panel** that pushes the chat (animated grid column, collapses when closed; toggled by the composer **References** button). Inline `[chunk:<id>]` citations render as clickable **sequential superscripts** (¹ ² ³ — per-message first-appearance order, deduped). The panel has a **paper switcher** (a tab per enabled session reference, overflowing to a "…" dropdown beyond 3). Clicking a citation resolves the chunk via `GET /chunks/{id}` → `{paper_content_id, section, text}`, switches the switcher to that paper, and **scrolls + highlights** the passage; opening via the References button browses the active paper with no highlight. **Document rendering (v2.14, library-based):** a paper rendered from LaTeX is shown as HTML embedded via an iframe **`srcdoc`** (same-origin → DOM-readable for highlight + dark-mode); a paper rendered from PDF (detected by `GET /papers/content/{id}/document` → `{mode}`; served by `GET /papers/content/{id}/pdf`) is shown inline via **`react-pdf`** from fetched bytes (no download). Content is fetched (CORS) + cached per paper, **prefetched** for all enabled refs on session load and surviving the session. **Locate-and-highlight is by content, not character offset** (v2.13): `chunks.char_start`/`char_end` index the extracted text (aligns only with `<pre>` fallback renders, not the pandoc DOM), so the canvas text-searches the live document for the chunk's stored `text` (whitespace-normalized prefix via `TreeWalker`), builds a `Range`, `scrollIntoView`, and applies a transient highlight (HTML only; PDFs carry an inline "highlighting unavailable" note, per §I-7 no-PDF.js). A failed search → a toast (NFR-02); a stale chunk (post-reingest `GET /chunks/{id}` 404) → an inline "passage no longer available — paper re-indexed" notice. Switching papers / closing happens without page reload. |
| **FR-04** | Compare-models view | Composer has a **Compare** toggle. When on, the same prompt fans out to two configured models; both responses stream into a split pane with their own routing badges + trace panels. Both branches share a single `run_id`; every `tool_calls` row written in either branch carries `branch='A'` or `branch='B'` so replay can reconstruct each side independently (see §III-7). |
| **FR-05** | Chat shell parity with `Intro2GenAI-hw1` | History sidebar with search; new-chat; conversation fork from any past message; thinking-block auto-disclosure for `<think>` tags; SSE streaming with token-by-token rendering; dark / light theme. |
| **FR-06** | LangGraph router (6 active intents) | Single LangGraph node classifies user input into `paper_search` / `paper_suggest` / `paper_qa` / `slides` / `library_stats` / `memory`, with `chitchat` as the fallback and a `clarify` short-circuit (v2.11) when even history can't resolve the turn. `paper_suggest` (v2.12) is the topic-recommendation sibling of `paper_search` (resolve a specific named paper); both share the v2.7 four-stage subgraph, swapping only the Parser + Synthesizer prompts (§III-3). `memory` (v2.16) routes explicit "remember… / update my note… / forget…" turns to a small memory node that recalls→decides→writes via the `memory` MCP (FR-10); it is distinct from the *autonomous* `memory.add`/`memory.recall` tool use available to other agents. Emits `routing_decision` SSE event before any sub-agent fires. Thin: one prompt + one structured-output call. |
| **FR-07** | In-repo pipelines (paper + slide) | **v2.7 update**: `paper_search` is no longer a single tool-calling loop — it is a **four-stage LangGraph subgraph** (`ps_parse` → `ps_process` [Discover→Resolve inner loop, fan-out per request via `asyncio.gather`] → `ps_finalize` → `Synthesizer`); see §III-3 Research Agent row for the full topology. The read-only contract, the 3-call combined `papers.search_semantic_scholar` + `web.*` cap, the `finalize`-marker semantics, the `search_results` + `final` SSE shape, and the three-attach-paths model below are all preserved from v2.4-v2.6. The remainder of this row describes the **read-only contract** that holds whichever internal topology is in use. — `paper_search` is a **tool-calling Research Agent loop** (v2.3) whose mission is to **extend the session's reference set** on the user's behalf, **informed by what's already in it** (most research builds on prior work: the agent must be able to see "this session already has Vaswani et al. 2017 — let me find follow-up MoE routing work that cites it"). Each invocation's prompt is constructed with a **current-references context block** — title, arxiv_id, year, and short abstract for every currently-enabled paper in the session — so the agent reasons about gaps and follow-ups before searching. Tool palette (v2.4 — **read-only**; the agent shortlists with optional finalize markers, the system + user drive ingestion): `search_library(query, max_results)` (full-text search across the user's deduplicated `paper_content` rows that aren't yet attached to the current session; returns title / arxiv_id / abstract / `paper_content_id`), `search_semantic_scholar(query, max_results)` (Semantic Scholar REST `/graph/v1/paper/search`; broader coverage than arXiv with citation counts, `externalIds.ArXiv` when present, `openAccessPdf.url` when present, year, authors), `find_related_papers(paper_id, mode: cites | cited_by | similar, max_results)` (Semantic Scholar citation graph for "follow-up to X" / "what does X cite" / "similar to X"; `paper_id` is either `arxiv:<id>` or `ss:<paperId>`). **No write tool — `add_paper_to_session` is not in the LLM-visible palette in v2.4**; the agent expresses confidence via an optional `finalize: true` marker on each candidate in the shortlist (capped server-side at **2 per turn** regardless of what the LLM emits — the chat endpoint truncates extra finalize markers to suggested-only). Finalize-flagged candidates are auto-attached by the chat endpoint before emitting the `search_results` SSE event; the remaining candidates render as suggested cards in the UI awaiting the user's Add click. **Only finalized + user-clicked candidates download** — suggested-only candidates are metadata only. The agent's job is to **shortlist** the best candidates and explain why; the chat endpoint emits a `search_results` SSE event with the shortlist after the agent finishes its loop. Search and citation-graph results carry enough metadata (`paper_id`, title, abstract, year, authors, `arxiv_id`, `has_open_pdf`, agent's `reason`) for the UI to render each as an Add-button row without re-fetching. `search_arxiv` is **not in the LLM-visible palette** in v2.4 either — it survives only as an internal Python helper inside the arXiv-source download path triggered by `POST /papers`. The download path is identical to v2.3 cache semantics: canonical `content_key` (`arxiv:<id>` or `sha256:<hex>` for PDF), so the same paper picked by the user from a Semantic Scholar card resolves to the same `paper_content` row regardless of discovery path. **Canonical per-turn flow** (the agent prompt encodes this priority order):
  1. **Query formable?** If the user's message + session context is too vague to form a search query, ask one clarifying question and stop (no tool call).
  2. **Library first.** Call `search_library(query)` — the user's already-indexed paper_content is cheaper than re-downloading anything.
  3. **Discover externally.** Call `search_semantic_scholar(query)` (v2.4 — was `search_arxiv` in v2.3); refine the query and call again up to **N=3 times per turn** if the initial result set is weak (hard limit, each call traced as a `tool_calls` row). For follow-up-work questions on a specific paper, call `find_related_papers(paper_id, mode='cited_by')` instead. **v2.5 (conditional on MCP-registry availability of the `web` server)** — when the user's query is short / vague / names a paper indirectly (e.g. "that diffusion paper everyone cites"), insert a **discovery step before refine**: call `web.search(query, limit)` against `Aas-ee/open-webSearch` (no-key multi-engine: DuckDuckGo / Bing / Brave / Startpage / Baidu / Sogou) with 1-2 broad queries to surface candidate paper titles from blog posts / READMEs / "papers I love" lists; optionally call `web.fetch(url, maxChars)` to extract the canonical paper title from a promising hit page; then call `search_semantic_scholar` with the refined title to land a citable paper. The `web.*` tools are **discovery-only** — their results carry **no `paper_id`**, never enter `recent_results`, and cannot appear in the `json:candidates` shortlist; the only path to a citable pick is round-tripping through `search_semantic_scholar` or `search_library`. The N=3 cap is **broadened to cover `search_semantic_scholar` + `web.search` + `web.fetch` combined** per turn (so the agent cannot burn the budget on web alone). When the registry has no `web` server (daemon down or not configured), the tools simply don't enter the palette and `paper_search/v1` loads instead of `paper_search/v2` — behaviour reverts to v2.4 exactly. MCP transport and registry shape: §III-6.
  4. **Shortlist + finalize + explain.** Pick the **best 3-5 candidates** across library + Semantic Scholar results. Mark at most **1-2 with `finalize: true`** — papers the agent is confident enough to auto-attach. Compose a final assistant message that names each pick with a one-line reason. **Do not call any write tool — the agent has no `add_paper_to_session` in v2.4.** The chat endpoint reads the finalize markers, auto-attaches those candidates via `add_paper_to_session_dispatch` (server caps at 2 even if the LLM marks more), then emits a `search_results` SSE event with each candidate's `finalize`, `auto_added`, `papers_id`, and `error` fields populated. The frontend renders each candidate as a card: finalize-flagged ones show "Added by agent ✓" with toggle/remove controls (same as user-added refs); suggested-only ones show an Add button the user clicks to ingest. Only finalized + user-clicked candidates download; pure suggestions stay metadata-only. Research often surfaces many tangentially-relevant papers; this hybrid keeps the agent's confident defaults useful while letting the user curate the long tail without wasting download budget. The agent must not propose papers that are already in the session — it sees them in the context block — and prefers library hits over arXiv when both satisfy the user's intent equally well. **Three layers of deduplication** make duplicate work structurally impossible even on agent error: (1) the prompt-side context block excludes already-attached papers from the candidate space; (2) `search_library` excludes anything already in the current session; (3) the DB enforces `UNIQUE (session_id, paper_content_id)` on `papers` and `UNIQUE (content_key)` on `paper_content`, so an attempted re-add is a no-op INSERT, and an attempted re-ingest is a cache-hit that skips download / extraction / chunking / embedding entirely. **The agent never toggles `papers.enabled`, never deletes `papers` rows, and never mutates `paper_content` / `chunks` directly** — those are user-only gestures exposed via deterministic UI affordances backed by REST endpoints (PATCH `/papers/{id}` for the enabled toggle, DELETE `/papers/{id}` to remove from the session, POST `/papers/from-library` to manually attach a previously-indexed paper). The Reference Sources panel is the single curation surface: every agent-added paper appears there alongside manually-added ones, each with enable / disable / remove controls so the user can adjust the agent's selection at any time. See FR-08. Cross-turn refinement uses `AgentState.history` — the user's reply to a clarifying question (or a remove gesture between turns) is seen by the next invocation. `paper_qa` uses the in-repo **Paper Pipeline** (§III-5): arXiv / PDF / LaTeX extraction (adapted from `paper2slides-plus` utilities), chunker, embedder, Chroma retrieval, cross-encoder rerank. `slides` uses the in-repo **Slide Pipeline** (§III-5): LangGraph-orchestrated structure planning + per-section generation on a modern framework (Marp / Slidev / Beamer-via-LangGraph). `library_stats` dispatches to the in-repo `sqlite` MCP. Nothing runs as an external `paper2slides-plus` service. |
| **FR-08** | Session-scoped reference sources | The reference set is built up through **three attach paths**, all routing through `add_paper_to_session_dispatch(paper_id)`: (1) **Agent-finalized**: the agent emits a candidate with `finalize: true` in the shortlist; the chat endpoint auto-attaches (max 2 per turn). (2) **User clicks Add on a SearchResultList card** → `POST /papers` with `paper_id` ∈ {`arxiv:<id>`, `ss:<paperId>`} → dispatcher resolves to arXiv source (preferred) or open-access PDF (fallback) or returns HTTP 422 `no_ingestible_source`. (3) **User attaches from the Library Browser** → `POST /papers/from-library` with `paper_content_id` (idempotent on `UNIQUE (session_id, paper_content_id)`, always cache-hit). The **Reference Sources** surface is recommended as a **collapsible drawer** (right-edge slide-out or sidebar-bottom collapsible) listing all attached papers for the active session joined to `paper_content` for title/year. Per-row controls: enabled toggle → PATCH `/papers/{id}`, remove → DELETE `/papers/{id}`. **The agent never invokes any of these endpoints** — its tool palette is read-only (see FR-07). The agent's only ingestion-related signal is the `finalize` flag on its shortlisted candidates; the chat endpoint enforces the cap (2) and is the actual caller of the dispatcher. Papers are **session-scoped at the membership/toggle layer** and **deduplicated at the content layer** (v2.2 two-layer design, §III-7). `papers (session_id, paper_content_id, enabled, added_at)` is the per-session membership row; `paper_content` owns the unique physical paper (one row per `content_key`, one on-disk dir under `workspace/papers_cache/...`, one set of `chunks` + Chroma vectors). The **Reference Sources** sidebar panel lists every `papers` row for the current session (joined to `paper_content` for title / year) with a toggle each. The `paper_qa` retrieval scope (FR-07) and the `slides` corpus (UC-4) **must** intersect Chroma queries with `{paper_content_id ∈ (SELECT paper_content_id FROM papers WHERE session_id = :s AND enabled = TRUE)}`. Toggling off flips `papers.enabled` only — it does not touch `paper_content`, the cache dir, or chunks. Adding a reference is the explicit follow-up to a `paper_search` result via an **Add as reference** button (UC-2); the Paper Pipeline performs a cache lookup on `content_key` (§III-5.1) — on hit it inserts a new `papers` row referencing the existing `paper_content_id` (no re-download, no re-extraction, no re-embedding); on miss it ingests the paper into the cache, inserts a `paper_content` row + `chunks`, then the `papers` row. The `UNIQUE (session_id, paper_content_id)` constraint prevents accidentally adding the same paper twice to the same session. |
| **FR-09** | Audit-log writer | Every model call, MCP call, and pipeline step writes a `tool_calls` row before returning, tagged with the active `branch` (empty string outside Compare mode; `'A'` / `'B'` inside it — FR-04). Redactor masks API keys (`sk-ant-*`, `sk-proj-*`, `AIza*`) and `$HOME` paths. Survives client disconnect (run finalises on `CancelledError`). |
| **FR-10** | Memory store (session + global) *(v2.16 + v2.17 governance)* | A persistent fact store (`memories` table, §III-7) with two scopes: `session` (bound to one `chat_sessions.id`) and `global` (`session_id = NULL`, cross-session). Accessed **only** through the dedicated write-capable `memory` MCP (§III-6) for agent operations — curation from the UI uses the REST endpoints (FR-11). Tools: `memory.recall(query, scope)` (FTS5 over `content`, `status='active'` filter), `memory.add(content, scope)`, `memory.edit(id, content)`, `memory.forget(id)`. **Scope is the NFR-05 boundary for writes:** a `scope='session'` write binds to the request-context `session_id`; `edit`/`forget` are ownership-checked (target must be `global` or this session's, else the call is rejected and its `tool_calls` row is `status='rejected'`). **Scope mapping (user vs project):** the LLM scope classifier maps **personal preferences/habits → `global` (user scope)** (e.g. "always answer in Traditional Chinese", "prefer concise replies") and **project/framework/DB/architecture settings → `session` (project scope)** (e.g. "this project uses FastAPI", "the DB schema uses MySQL"). `session` == project scope; `global` == user scope — the two scope values are the functional "user vs project" distinction. **Two write triggers:** (a) user-explicit via the router `memory` intent (FR-06); (b) agent-autonomous via `memory.add` available to `paper_qa` / `library_stats` (capped ≤2 `add`/turn). **Memory Gate (v2.17, rule-based):** every `memory.add` path — both explicit and autonomous — runs through a deterministic safety gate (`agents/memory_gate.py`) before any LLM classification. The gate refuses **sensitive** content (API keys: `sk-...`/`AIza...`/similar patterns; passwords; credit-card/ID numbers; medical/diagnosis text; salary; customer PII) and **dangerous** content ("skip validation", "disable security", "ignore rules", "bypass review") with a visible reason to the user and `status='rejected'` in the `tool_calls` row — even when the user explicitly requests storage. Rules own this boundary (§II-1 #3); an LLM classifier would have non-zero false-negative rate. **Status lifecycle (v2.17):** `memories.status ∈ {'active','superseded'}` (default `active`). On `memory.add`, a small LLM conflict-detection call compares the new content against active same-scope memories; if a contradiction is found, the new memory saves with `supersedes=<old-id>` and the old row flips to `status='superseded'` + `superseded_by=<new-id>`. This is LLM-driven (§II-1 — creative judgement whether two memories conflict) not rule-driven. **Active-only recall:** `recall_memories`, `build_memory_context_block`, and `memory.recall` all filter to `status='active'`. Superseded memories are never injected into agent context. **Recall injection on by default:** a `memory_recall` step injects top-k active session+global memories into the `paper_qa` finalizer + `library_stats` agent context. Semantic recall (embed memories → Chroma) is an opt-in upgrade path, off by default. |
| **FR-11** | Memory Manager UI *(v2.17)* | A frontend panel (`MemoryManager.tsx`) opened via a Composer or sidebar control. Lists all memories for the active session and all global memories — both active and superseded — grouped by scope with labels **"User (global)"** and **"Project (session)"**, so the user/project distinction (§III-2 MemoryManager, FR-10 scope mapping) is visible in the UI without renaming the shipped `scope` enum values. (`global` == user-scope; `session` == project-scope.) Each row shows: content, scope badge, status badge (active / superseded), created/updated timestamps, and supersede-chain links (`supersedes`/`superseded_by` ids with content preview on hover). **Per-row controls:** edit content inline → `PATCH /memories/{id} {content}`; delete → `DELETE /memories/{id}`; toggle active↔superseded → `PATCH /memories/{id} {status}`; optional manual add → `POST /memories {content, scope}`. **REST endpoints** (backend `api/memories.py` router, registered in `app.py`; NOT MCP — deterministic user curation, not agent action): `GET /memories?session_id=<id>` (returns session-scoped + all global memories, both statuses, with supersede ids); `PATCH /memories/{id}` (content and/or status, ownership-checked: same rules as `memory.edit` — global or this session's only); `DELETE /memories/{id}` (same ownership check). These endpoints mirror the `papers` PATCH/DELETE pattern (FR-08) — same auth posture, same deterministic ownership rules. The panel and its api-client/store slice follow the Reference Sources panel pattern. |
| **FR-12** | Slide deck experience *(v2.18)* | The session's Beamer deck (UC-4, §III-5.3) is viewed, presented, and edited through a **Slides panel** — a new right-panel canvas sharing the right slot with the Citation Canvas + Memory Manager (opening one closes the others). Layout: a **filmstrip navigator** (jump to any slide) · the **current slide** rendered from the compiled PDF via `react-pdf` (the Plan D dependency) · a **collapsible speaker note** for the current page, with a **draggable horizontal divider** between slide and note (vertical resize, same handle pattern as the Canvas width resize). Opened by a **deck chip** in the assistant message (rendered on the `deck` SSE event: slide-1 thumbnail + title + page count + Open / Present / Download). The panel keeps `current_view_page` live and attaches it to the next `slides` turn so the Report Agent resolves edit scope (§UC-4, §III-3). **Version history:** a History control lists snapshots and restores one (`GET`/`POST /sessions/{id}/deck/versions…`). **Presentation mode:** a Present control keeps the chat + panel mounted and enriches the panel into a presenter view (elapsed timer, next-slide preview, sync badge), and opens `/present/<session_id>` in a **separate window** — **slide-only, fullscreen, zero chrome** for the second screen. Page sync is **`BroadcastChannel('paperhub-present-<sid>')`** (no backend round-trip): every page change broadcasts `{page}`; the audience window follows but holds its own state, so closing the panel to answer an audience question (a normal `paper_qa` turn — Citation Canvas) leaves the presentation on its page and `current_view_page` persists for when the panel reopens. **States (NFR-02):** streamed generation progress (`tool_step`), compile-error → last good PDF + banner + Trace link, empty enabled set → "enable a reference to generate slides". REST surface in `api/decks.py`; a `slides` Zustand store holds `{deckMeta, currentPage, presenting}`, prefetched on session load for sessions that have a deck (mirrors `useReferencesSync`). A static UI mockup: `docs/superpowers/mockups/plan-F-slides-ui.html`. **v2.21 — decoupled, opt-in operations:** the deck is generated **slides-only**; the finalize message hints the next moves and the deck chip exposes affordances to **generate speaker notes** (opt-in), **edit a slide** (current / specific page / whole deck), and **edit notes** — all expressed as ordinary follow-up chat turns resolved by the deck-command classifier (§III-5.3). **Length is a user input** (e.g. "a 20-minute talk", "15 slides"; default 20 min ≈ 15 slides). **Note language is independent of the slide language**, so the deck can stay English while the notes are Traditional Chinese. All three follow-up operations are **ordinary chat turns** routed by the deck-command classifier (§III-5.3) — the deck-chip buttons ("Generate notes", "Edit") simply compose and send the corresponding chat message (e.g. "generate speaker notes", "edit this slide"), reusing the existing `/chat` SSE stream + trace; **no new REST surface** beyond the existing read-only `api/decks.py` (`GET /deck`, `/deck/pdf`, `/deck/tex`). |

## I-6. 非功能需求 (Non-Functional Requirements — 6)

| ID | Category | Target |
| --- | --- | --- |
| **NFR-01** | UX latency | First token ≤ 5 s warm for `paper_qa` and `library_stats`; ≤ 2 s for `chitchat`. Citation Canvas scroll-and-highlight ≤ 200 ms after click. |
| **NFR-02** | UX completeness | Loading, empty, error, and rejection states each have a visible UI representation. No silent failures; no "lost" assistant bubbles after a stream error. |
| **NFR-03** | Reuse posture | The reference projects are **decomposed, not consumed.** Useful extraction / LaTeX helpers from `paper2slides-plus` are copied into the repo and adapted (with provenance comments); the chat-with-paper and slide orchestrators are intentionally rebuilt. UX idioms from `Intro2GenAI-hw1` are ported to React. New backend Python target ≤ ~3k LoC (Paper Pipeline ≈ 800, Slide Pipeline ≈ 600, agents + tracer + router ≈ 900, FastAPI surface ≈ 400). Provenance comments required on every copied file. |
| **NFR-04** | Auditability | Every chat turn → unique `run_id`. Full `tool_calls` DAG replays from SQLite alone. API keys + home-dir paths redacted. |
| **NFR-05** | MCP scope boundary | Every MCP tool declares a scope (filesystem root, table allowlist, URL domain allowlist). Out-of-scope arguments rejected at the orchestrator, logged as `tool_call.status='rejected'`, surfaced in the Trace panel. |
| **NFR-06** | Strict typing on new code | New Python uses Pydantic v2 + `mypy --strict`. All cross-module signatures (router → agent, agent → pipeline, pipeline → tracer) typed via Pydantic v2 models. Frontend uses TypeScript strict mode. |

## I-7. 系統邊界 (Out of Scope)

- No relation graph, no research-direction suggestion, no project / tag / reading-status workflows.
- No PDF.js viewer — the Citation Canvas renders the pre-rendered HTML produced at import time (§III-5.1, `paper_content.html_path`), not a PDF page. Full PDF rendering is post-demo.
- No LLM-as-judge harness. Evaluation is hand-graded markdown.
- No CI gate on the eval rubric.
- No multi-user, no auth, no cloud deployment.

## I-8. 驗收標準 (Acceptance Criteria)

Tied 1:1 to the four product dimensions from §I-1 plus the Citation Canvas headline.

1. **Routing.** On a 16-prompt fixture (4 per active intent), Router top-1 accuracy ≥ 80 %. Every out-of-scope MCP call attempted in the demo (e.g. `sqlite` write, `..` filesystem traversal) is rejected and renders yellow in the Trace panel.
2. **Citation Canvas.** For ≥ 5 distinct inline citations in the demo deck, clicking opens the canvas, scrolls to the chunk, and highlights it within ≤ 200 ms on the demo laptop.
3. **Multi-paper Q&A.** A comparative question over ≥ 2 indexed papers yields an answer citing chunks from ≥ 2 distinct `paper_content.id` (i.e. two genuinely different papers, not two `papers` rows that happen to reference the same content).
4. **Trace.** Every chat turn produces a `run_id`; replay-from-SQLite reconstructs the full step list.
5. **Model comparison.** The Compare view renders two model outputs side-by-side for the same prompt, each with its own routing badge + trace. A hand-graded markdown table (10 prompts × 2 models) lives in `eval/results/`.
6. **No silent failure.** A scripted error case (e.g. the LLM provider returns 500, or the Chroma index is missing) shows a visible error in chat AND a red row in the Trace panel — verified once during demo dry-run.

---

# Part 2 — 技術選型分析 (Technology Selection Analysis)

## II-1. 哪些地方「不能只用 LLM」？

Three places where the LLM is the wrong tool and rules / deterministic code must own the contract:

1. **Citation chunk resolution.** When the user clicks an inline citation, the canvas must scroll to the exact `chunks.id` row in SQLite. The LLM cannot invent that ID — the retriever's output supplies it, then the React renderer rewrites the citation marker into a canvas action keyed on the real chunk ID. Asking the LLM to "remember which chunk it cited" would yield plausible-but-wrong IDs and the canvas would scroll to the wrong text.
2. **Tool-call audit log.** Every entry in `tool_calls` (latency, token counts, status, args) is a measurement, not an opinion. The tracer is rule-based: stopwatch around the call, `try / except` around the return, regex over `args_redacted`. If the LLM judged whether a step "succeeded", the audit log would lose its evidentiary value the moment the LLM disagreed with reality.
3. **MCP scope enforcement.** The rejection of a filesystem `..` traversal, a `DROP TABLE`, or a query against a non-allowlisted table is a hard security boundary. An LLM "scope checker" would have non-zero false-negative rate; rules (regex / `Path.resolve()` / `sqlglot.parse_one`) guarantee 100 % rejection on declared scope.

## II-2. 哪些地方應該用「規則」反而比較好？

| Task | Rule | Why it beats an LLM |
| --- | --- | --- |
| arXiv ID validation | regex `^\d{4}\.\d{4,5}(v\d+)?$` | 100 % accurate, microseconds, zero cost. |
| SQL safety check (NL2SQL output) | `sqlglot.parse_one(sql)` + verb allowlist `{SELECT, WITH}` + table-name allowlist | Deterministic AST inspection — LLM "is this safe?" has non-zero failure rate. |
| Filesystem `..` rejection | `Path.resolve().is_relative_to(workspace_root)` | One canonical answer per path; no probabilistic judgement. |
| API-key redaction in `tool_calls` | regex over `sk-(ant\|proj)-…` and `AIza…` | LLM redactor would miss novel key shapes and is expensive. |
| Routing confidence threshold | numeric `confidence < 0.6 → fallback_to_user=true` | Repeatable, not stochastic. Easy to A/B. |
| Citation chunk lookup | `SELECT … FROM chunks WHERE id = ?` | One canonical answer per ID. |
| Intro2GenAI-hw1 `<think>` block disclosure | DOM regex over `<think>…</think>` | Already works in the reference repo; no model needed. |

**Principle.** Any task with a single deterministically-judgeable correct answer that must be auditable is owned by rules. LLMs are reserved for the semantic / generative work — drafting summaries, routing classification, NL2SQL translation — where multiple acceptable outputs exist and creativity matters more than exactness.

## II-3. 如果不用 RAG，會發生什麼問題？

Five concrete failure modes — each one tied to a specific FR that would break:

1. **Hallucination on the user's library (breaks FR-02 / UC-2).** The user's freshly-indexed papers are not in any LLM's training data. Without RAG, "compare these two papers on expert collapse" returns a plausible-sounding answer composed from the field's *average* paper — and cites section numbers that don't exist.
2. **Citation Canvas dies (breaks FR-03).** The headline UX feature exists only because the retriever attaches a real `chunks.id` to every passage. Without RAG there are no chunk IDs to scroll to; clicking a citation does nothing.
3. **Context-window blowup on multi-paper Q&A (breaks NFR-01 / NFR-05).** Two full 30-page papers ≈ 50 000 input tokens. Stuffing them into every Q&A prompt blows past Gemini Flash's effective practical input ceiling, costs USD ¢×N per turn, and pushes first-token latency well past the 5 s budget.
4. **Knowledge cutoff (breaks UC-1).** A paper published last month doesn't exist in any model's parametric memory. Without RAG over the user's freshly-indexed library, the system cannot answer about anything new.
5. **Personalisation lost (breaks the value proposition).** PaperHub's edge over generic ChatGPT is "the papers *I* have read". Without RAG, the system has nothing personal to retrieve from — it degrades to a generic chat wrapper, and the citation-rate acceptance criterion (I-8 #3) cannot be met.

**Summary.** RAG is not an optional optimisation — it is the architectural decision that lets the Citation Canvas, the source-cited answer, and the personalised library experience all work. The actual RAG pipeline (chunking, embedding, retrieval) is the **in-repo Paper Pipeline** (§III-5.1); the Research Agent threads the retrieved chunk IDs through to the UI as `[chunk:<id>]` markers so the Citation Canvas can resolve every click against a real `chunks` row.

---

# Part 3 — 系統架構設計 (System Architecture Design)

## III-1. Overview

```
┌────────────────────────────────────────────────────────────────────────┐
│  ① UI Layer — React + Vite + Tailwind                                  │
│   ├─ Sidebar       — history, current papers, search, fork             │
│   ├─ Chat pane     — streaming, RoutingBadge, TraceInline, Compare     │
│   └─ Citation Canvas — paper viewer w/ chunk highlight (right pane)    │
└─────────────────────────────┬──────────────────────────────────────────┘
                              │ REST + SSE
┌─────────────────────────────▼──────────────────────────────────────────┐
│  ② Orchestrator / Workflow — FastAPI + LangGraph (new, ~3k LoC)        │
│   ├─ Router Agent   — 4-intent classifier (structured output)          │
│   ├─ Research Agent — paper_qa node; in-repo RAG over Chroma chunks    │
│   ├─ SQL Agent      — library_stats node; NL2SQL via sqlite MCP        │
│   ├─ Report Agent   — slides node; in-repo Slide Pipeline              │
│   └─ Tracer         — writes every step to tool_calls                  │
└────┬─────────────────┬──────────────────┬─────────────────┬────────────┘
     │                 │                  │                 │
┌────▼─────────┐ ┌─────▼──────────────┐ ┌────▼──────────────┐ ┌────▼─────────┐
│ ③ LLM Module │ │ ④ RAG / KB         │ │ ⑤ External Systems│ │ ⑥ Database   │
│  LiteLLM     │ │ Paper Pipeline →   │ │  MCP servers:     │ │ SQLite       │
│  adapter     │ │ Chroma index +     │ │   filesystem      │ │ 9 tables     │
│  Gemini /    │ │ cross-encoder      │ │   sqlite (own)    │ │ Chroma       │
│  Anthropic / │ │ rerank             │ │   paperhub.* (own)│ │ chunk vectors│
│  Ollama      │ │ Slide Pipeline →   │ │                   │ │ Local FS     │
│              │ │ Beamer (LaTeX) →   │ │  arXiv:           │ │ (PDFs, .tex, │
│              │ │ pdflatex compile   │ │   copied utility, │ │  e-prints)   │
│              │ │ (uses adapted p2s  │ │   not an MCP      │ │              │
│              │ │  extract utils)    │ │                   │ │              │
└──────────────┘ └────────────────────┘ └───────────────────┘ └──────────────┘
```

## III-2. ① UI Layer — Custom React, Open WebUI style

React + Vite + Tailwind + Zustand store. Layout mirrors `Intro2GenAI-hw1` (collapsible sidebar; centre chat pane; bottom composer) with two structural additions: a **Reference Sources** panel under the sidebar, and a right-side drawer for the **Citation Canvas**.

Components and where each comes from:

| Component | Provenance |
| --- | --- |
| `Sidebar` | Port from `Intro2GenAI-hw1` (history list, search, fork affordance) |
| `ReferenceSourcesPanel` | **New** — under the sidebar; lists every `papers` row scoped to the active `session_id` (joined to `paper_content` for `title` / `year` / `arxiv_id`), each with the `enabled` **toggle**, "Open in Canvas", and "Remove from session" affordances. Scopes every downstream `paper_qa` and `slides` invocation. |
| `Composer` | Port from `Intro2GenAI-hw1` + new **Compare** toggle. (Adding a paper is done via UC-2 from a `paper_search` result, not a separate composer button.) |
| `MessageBubble` | Port from `Intro2GenAI-hw1` (streaming markdown, `<think>` disclosure) |
| `RoutingBadge` | **New** — chip on assistant header |
| `TraceInline` | **New** — collapsible `tool_calls` list with status colouring |
| `CitationCanvas` | **New (headline)** — right-pane drawer, smooth-scroll + highlight |
| `CompareSplit` | **New** — two-pane variant of chat layout |
| `SearchResultList` | **New** — rendered inline after a `paper_search` turn; each row carries metadata + an **Add as reference** button (UC-2 entry point) |
| `EmptyState` / `LoadingDots` / `ErrorToast` / `RejectionPill` | All four states must be present per NFR-02 |
| `MemoryManager` | **New (v2.17, FR-11)** — panel listing session + global memories grouped by scope with section labels **"User (global)"** and **"Project (session)"** (making the user/project distinction visible in the UI without renaming the `scope` enum values: `global` == user-scope; `session` == project-scope), active/superseded badges, supersede-chain links, per-row edit/delete/toggle controls. Opened via a Composer or sidebar trigger. Mirrors the Reference Sources panel pattern. API client + store slice follow the `papers` PATCH/DELETE conventions |

## III-3. ② Orchestrator / Workflow — FastAPI + LangGraph

A single `POST /chat` SSE endpoint. Builds a LangGraph state machine per request, routes via the **Router Agent**, dispatches to one of the sub-agents — **Research Agent / SQL Agent / Report Agent / Memory node** — or the `chitchat` fallback; emits SSE events `routing_decision`, `tool_step`, `token`, `citation`, `search_results`, `deck` (v2.18 — slides), `final`, `error`.

The agents below are first-class LangGraph nodes — discrete components with their own state, prompts, and tool palette (the Router classifies; the Research / SQL / Report agents + the Memory node handle their intents):

| Agent | Role | Dependencies |
| --- | --- | --- |
| **Router Agent** | Classifies intent → `paper_qa` / `library_stats` / `slides` / `paper_search` / `paper_suggest` / `memory` (fallback `chitchat`; `clarify` short-circuit); picks model tier; resolves anaphora into `resolved_query` + detects `response_language` (v2.11/v2.13); emits the `routing_decision` event | LLM (small tier) |
| **Research Agent** | Handles `paper_search` as a **four-stage LangGraph subgraph** (v2.7 — decomposed from the v2.4-v2.6 single-LLM-turn loop; the read-only contract + shortlist + finalize-marker semantics are preserved, only the internal topology changed). Stages: **(1) `ps_parse`** — small-model LLM splits the user message into N `ParsedRequest`s (deterministic arxiv-ID / DOI pre-scan short-circuits common cases). **(2) `ps_process`** — fan-out per `ParsedRequest` via `asyncio.gather`; each branch runs a bounded Discover→Resolve inner loop (`MAX_REFINEMENT_LOOPS=1`). The **Discoverer** has only `paperhub.search_web` (a structured-output wrapper around `web.*` that makes free-text quoted queries structurally impossible — fields are `paper_hint: str` + `extra_terms: list[str]`, never `query: str`); the **Resolver** has only `papers.search_semantic_scholar` and calls it **exactly once** per request (architectural, not a prompt rule). When SS misses but the Discoverer surfaced an arxiv URL, Python synthesises a `ResolvedPaper` from the extracted `arxiv_id` rather than declaring NotFound. **(3) `ps_finalize`** — Python builds `SearchCandidate`s from `ResolvedPaper`s and emits a `SearchResultsYield` deterministically (the LLM never authors `json:candidates`; block emission is structurally guaranteed). The chat layer applies the finalize cap (max 2) and auto-attaches via `add_paper_to_session_dispatch`. **(4) `Synthesizer`** — short prose summary only, ~30-line prompt. Tool-step streaming is preserved end-to-end: each node drains `drain_tool_calls_since` between stages and emits `tool_step` custom events so the frontend trace panel updates per-stage rather than batched at end. The mission stays *"extend the session's reference set on the user's behalf, informed by what's already in it"* — the prompt context block of currently-enabled references is now consumed by `ps_parse` for disambiguation. Three tools (all read; same JSON-schemas as v2.4-v2.6) form the base palette: `search_library(query, max_results)` (search the user's deduplicated `paper_content` library, excluding papers already in this session), `search_semantic_scholar(query, max_results)` (Semantic Scholar `/graph/v1/paper/search`, primary external discovery), `find_related_papers(paper_id, mode, max_results)` (Semantic Scholar citation graph). **v2.5** — when the MCP registry has a reachable `web` server (`Aas-ee/open-webSearch`, optional external dependency — §III-6), two **discovery-only** tools join the palette: `web.search(query, limit, engines)` (no-key multi-engine general web search; DDG / Bing / Brave / Startpage / Baidu / Sogou) and `web.fetch(url, maxChars)` (fetch + readable extraction of a result page). They surface candidate paper *titles* from blog posts / READMEs / "papers I love" lists when the user's query is short / vague / names a paper indirectly; results carry no `paper_id` and cannot enter `json:candidates` directly — the agent must round-trip a discovered title through `search_semantic_scholar` (or `search_library`) to produce a citable pick. The existing 3-call external-search cap covers `search_semantic_scholar` + `web.search` + `web.fetch` **combined**. With the daemon down or the server unconfigured, the palette reverts to the three v2.4 tools and the prompt registry loads `paper_search/v1` instead of `paper_search/v2`. **No write tool in v2.4** — `add_paper_to_session` was removed from the LLM palette. The agent expresses commitment via `finalize: true` markers in its shortlist; the chat endpoint auto-attaches finalized picks (max 2) before emitting `search_results`. `search_arxiv` is also not in the LLM-visible palette; it survives only as an internal Python helper inside the arXiv-source download path. Per invocation the agent reads `AgentState.history + user_message + current-references context block` (titles, arxiv_ids, abstracts of all currently-enabled session references) and decides whether to (a) emit a clarifying question and stop, (b) `search_library` and shortlist library hits, or (c) `search_semantic_scholar` up to N=3 times with refined queries and shortlist external hits. The agent's final assistant message names the 3-5 picks with reasons + finalize flags; the chat endpoint emits a `search_results` SSE event with the structured candidate list ({paper_id, title, authors, year, abstract, arxiv_id, has_open_pdf, reason, finalize, auto_added, papers_id, error}) just before `final`. The frontend renders these as a `SearchResultList`: finalize-flagged rows show "Added by agent ✓"; the rest show Add buttons the user clicks to ingest. **Three sources of an attached paper**: agent-finalized auto-attach, user clicks Add on a suggested card, user attaches from Library Browser. The agent does not propose papers already present in the context block. Each tool invocation is wrapped by the Tool-Call Tracer (FR-09). The final assistant message names which papers were auto-added (now enabled in the session) and which remaining results the user can attach manually via **Add as reference** buttons on the `SearchResultList` rows. **The agent never toggles `enabled`, never deletes `papers` rows, never mutates `paper_content` / `chunks` directly — see FR-07.** Toggle / remove / manual-attach-from-library are user gestures backed by REST endpoints, used to adjust what the agent extended. Cross-turn refinement is automatic — the next invocation sees the prior conversation and the current curated reference set. Also handles `paper_qa` via the **v2.10 agentic hierarchical pipeline** (§III-5.2): one per-paper subagent per enabled reference navigates that paper's section TOC (`list_sections` / `read_section`, small-tier model) and cites chunks; a flagship finalizer composes the answer over the raw cited chunks. Surfaces `[chunk:<id>]` markers so the Citation Canvas can resolve them. | Paper Pipeline (§III-5), Chroma, LLM (flagship finalizer + small-tier per-paper subagent) |
| **SQL Agent** | Handles `library_stats` (v2.16 — replaces the Plan A `_stub_library_stats` node). Introspection-driven NL2SQL over the read-only `sql` MCP: a small-model call uses `sql.list_tables` + `sql.describe(table)` to learn the schema, emits a `SELECT`, runs it via `sql.query` (which `sqlglot`-validates: verb allowlist `{SELECT, WITH}` + table allowlist, out-of-scope → `status='rejected'`), and **self-repairs once** on error/empty (re-prompt with the error + schema). A flagship call then phrases the answer in `response_language` with the executed SQL embedded as a code block. **Read-and-act:** matching `paper_content` rows can be shortlisted as `library:<id>` candidates and auto-attached (≤2, `finalize:true`) via the same `search_results` SSE path the Research Agent uses (UC-5, FR-08). Also carries the autonomous `memory.add` / `memory.recall` tools and receives the recalled-memory context block (FR-10, active-only). **Two-layer scoping (v2.17):** the planner prompt distinguishes "library" = `paper_content` (all papers the user has ever indexed, deduplicated) from "this chat's references" = `papers ⨝ paper_content WHERE papers.session_id = :session_id`; both table schemas (columns + types) are injected up front so the LLM does not hallucinate columns. Slots `sql_planner/v1` + `sql_repair/v1`. | `sql` MCP (read), `memory` MCP, LLM (small planner + flagship phrasing) |
| **Memory node** | Handles the `memory` intent (v2.16): a small-model loop that recalls candidate memories (`memory.recall`), decides the op (add / edit / forget) + scope (session / global), and writes via the `memory` MCP (ownership-checked; violations → `status='rejected'`). Distinct from the autonomous `memory.add` calls other agents make mid-turn. **Memory Gate (v2.17):** every add path — explicit and autonomous — runs through `agents/memory_gate.py` (deterministic rule check) BEFORE any write; sensitive or dangerous content is refused with a visible reason. **Conflict-supersede (v2.17):** on add, a small LLM call checks active same-scope memories for contradictions; conflicting old memory is flipped to `status='superseded'` and the new memory records `supersedes=<old-id>`. | `memory` MCP, LLM (small) |
| **Report Agent** | Handles `slides`. **v2.21 — three decoupled sub-flows (GENERATE slides-only / NOTES opt-in / EDIT diff-edit), routed by a deck-command classifier; notes are opt-in and independently languaged; length is a user budget; per-frame `deck_slides` rows. See §III-5.3 for the authoritative flow.** **v2.19 — PhD-grade rewrite (the v2.18 plan→sections→no-op-revise→notes chain below is NUKED; see §III-5.3 for the authoritative current flow):** a traced LangGraph subgraph that reads each paper's `PaperAsset` (figures+captions, equations-LaTeX, structured sections — §III-5.1) and runs `sl_resolve → sl_understand (per-paper) → sl_narrate (synthesis outline, figures assigned only from the real inventory) → sl_draft (per-slide concise-frame ⟂ rich-note pairs) → sl_coherence → sl_assemble (stage assigned figures under deck-unique keys) → sl_verify_figures (deterministic: no non-existent \includegraphics ever) → sl_compile (loop; Overfull-vbox is a fixable failure) → sl_notes_finalize → sl_emit`. Three hard contracts: concise slides + rich notes, no hallucinated figures, self-correcting layout. Cost no object (flagship throughout). Driven from `chat.py` via a `report_stream` shim. — *(historical v2.18 design, superseded:)* A traced **LangGraph subgraph** (`agents/report_graph.py`, same shape as the Research subgraph; driven from `chat.py` via a `report_stream` shim). Reads the session's enabled reference sources and the existing `decks` row, then `sl_resolve` (small LLM) classifies the turn into `create` / `recreate` / `edit`; on `edit`, the **LLM resolves page-vs-deck scope** from `current_view_page` (the slide on screen — not a user toggle). **Create/recreate path:** `sl_plan` (flagship — one thematic cross-paper **synthesis/survey** `SlidePlan` over enabled papers' abstracts + `sections_json`) → `sl_sections` (`asyncio.gather` fan-out, one RAG-grounded LLM call per planned section, chunks filtered to that section's source papers) → `sl_assemble` (deterministic: Beamer preamble w/ `metropolis` theme + fallback, merged-deduped `ADDITIONAL.tex` macros, `\graphicspath` across every contributing paper's `papers_cache` source dir) → `sl_compile` (reused `try_compile_with_fixes` revise-on-error loop, bounded retries) → `sl_notes` (one speaker note per PDF page) → `sl_emit` (writes `decks` row, snapshots a version, emits the `deck` SSE event). **Edit path:** deck-edit → `sl_edit_plan` (LLM patch plan: insert/replace/delete frames) → regenerate only affected frames + splice via `beamer_utils.replace_frame_in_beamer`; page-edit → `sl_edit_frame(N)` (single-frame rewrite + speaker-note shift). The framework is **Beamer-via-LangGraph** (v2.18, §III-5.3 deferred decision resolved) for conference-ready output. Reuses LaTeX helpers + version-history + speaker-note gen copied from `paper2slides-plus`; the orchestrator is new. Honors the active-memory block + `response_language` for chat prose like every answering agent (deck content stays in the papers' language). | Slide Pipeline (§III-5.3), Chroma, `pdflatex`, LLM (small `sl_resolve` + flagship plan/section/notes) |

`AgentState` is a `TypedDict` carrying `run_id`, `branch` (`''` outside Compare mode; `'A'` / `'B'` inside it — FR-04), `session_id`, `user_message`, `routing_decision`, `enabled_paper_content_ids` (resolved at the start of the turn from `SELECT paper_content_id FROM papers WHERE session_id = :s AND enabled = TRUE` — the membership → content lookup of FR-08), `retrieved_chunks`, `sql_result`, `recalled_memories` (v2.16 — top-k **active** session+global memories injected into `paper_qa` / `library_stats`; v2.17: only `status='active'` rows are ever included), `report_artifact_path`, `current_view_page` (v2.18 — the slide the user is viewing in the Slides panel, supplied by the frontend so `sl_resolve` can resolve edit scope), `report_decision` (v2.18 — the `sl_resolve` action + edit-target), `final_response`. Each agent is one graph node; the Router's output is a conditional edge. The **Tool-Call Tracer** is wrapped around every model call, MCP call, and pipeline step and writes a `tool_calls` row before returning.

**Framework choice rationale.** Two candidate frameworks were evaluated: **LangGraph** and **Google ADK**. LangGraph was picked because: (a) its node-graph metaphor makes the tool-selection logic visually unambiguous; (b) richer MCP-adjacent Python ecosystem; (c) LangGraph state diffs are diff-friendly in the audit log. Google ADK remains a drop-in alternative — the adapter boundary is the `LlmAdapter` Protocol, swap-out is one module.

## III-4. ③ LLM Module

LiteLLM-based provider adapter — uniform `generate(prompt, model_tier, response_model, slot)` interface; routes to Gemini, Anthropic, OpenAI, or local Ollama based on env config. Prompts live in a YAML registry with versioned slots (`router/v1`, `research_qa/v1`, `sql_planner/v1`, `sql_repair/v1`). Per-call cost is estimated via `litellm.completion_cost` and surfaced in the trace.

Two model tiers:
- **small** — Router intent classification, NL2SQL planner. Latency-sensitive.
- **flagship** — Research Agent answer generation, SQL repair, model-comparison sweep.

## III-5. ④ Knowledge Pipelines (Paper + Slide) & RAG

Two in-repo pipelines own everything between raw arXiv / PDF / LaTeX inputs and the final answer or deck. Both write `tool_calls` rows for each step so the audit trail is complete.

### III-5.1 Paper Pipeline (input → chunks, cache-aware)

The pipeline begins with a **cache-lookup gate**: on hit it short-circuits to the membership-row insert, skipping every compute stage. This is the storage + compute optimisation introduced in v2.2.

| Stage | Implementation | Reuse |
| --- | --- | --- |
| **arXiv search** | `arxiv` Python client wrapped in a small async helper | Adapted from `paper2slides-plus/src/arxiv_utils.py` (copied + edited) |
| **Compute content_key** | `arxiv:<arxiv_id>` for arXiv inputs; for raw PDFs / LaTeX-zip uploads, stream-hash the file with `sha256` and use `sha256:<hex>`. Resolves the input to a stable cache identity. | New |
| **Cache lookup** | `SELECT id FROM paper_content WHERE content_key = ?`. **On hit:** skip every downstream stage; the Paper Pipeline emits a single `tool_calls` row `paper_pipeline.cache_hit` and returns the existing `paper_content_id`. **On miss:** continue. | New |
| **Source download** | `arxiv.Result.download_source()` for e-print archives; HTTP GET for raw PDFs; ZIP extract for LaTeX projects; routed by input shape. Writes into `workspace/papers_cache/{arxiv/<arxiv_id>,upload/<sha256>}/source/`. | Adapted from `paper2slides-plus/src/file_utils.py` |
| **Structured extraction → `PaperAsset` (v2.19; async two-tier v2.20)** | Two extractors, one output contract; Marker is a **preferred-when-present add-on, never on the synchronous path** (v2.20). **arXiv with e-print source:** LaTeX preamble strip + `\section{…}`-aware splitting + real figure files + `\caption` text + equations (highest fidelity, fast → done synchronously; `asset_status='latex'`; adapted from `latex_utils`/`arxiv_utils`). **PDF sources (uploads, or arXiv with no source):** synchronous extraction is **PyMuPDF-only** — text + font-size heading sections + embedded/page images → a *degraded-but-instant* `PaperAsset` (no equation LaTeX) so the upload returns in seconds; `asset_status='pymupdf_only'`, or `'marker_pending'` if the `marker` service (§III-6) is reachable. A **single sequential background worker** then drains `marker_pending` papers (resumes on startup): `POST /extract` (PDF bytes → Marker `PdfConverter` JSON: blocks with `section_hierarchy`, `BlockTypes.Figure/Picture` = cropped image + adjacent caption + page bbox, `BlockTypes.Equation` = LaTeX, tables) **upgrades the on-disk `PaperAsset` in place + re-chunks + re-embeds** (see Chunking) → `asset_status='marker_ready'`; failure → `'marker_failed'` (PyMuPDF baseline retained). `PAPERHUB_MARKER_MAX_PAGES` default 1 (VRAM scales with page content density). Both extractors normalize to a unified **`PaperAsset`** in the cache dir: `asset/figures/fig-NNN.png` + `figures.json` (`{id, caption, page, section}`), `equations.json` (`{id, latex, section}`), `structure.json` (ordered sections). | `marker` (datalab-to/marker, Dockerized, optional) for PDF upgrade; PyMuPDF baseline; `paper2slides-plus/src/{latex_utils,arxiv_utils}.py` for arXiv |
| **Chunking** | Token-windowed greedy chunker over the structured blocks (v2.19 — section-aware boundaries; `cl100k_base`, target 800 / hard 1000). Runs **synchronously** off the PyMuPDF heading sections for PDFs (functional immediately) and off `\section{…}` for LaTeX; the background Marker worker **re-chunks + re-embeds** PDF papers off Marker's cleaner `structure.json` when the upgrade completes (better agentic-RAG section attribution). | New |
| **Embedding** | `sentence-transformers BAAI/bge-small-en-v1.5` (lazy-loaded; 384-dim). **v2.7**: loaded with `device=resolve_device()` (CUDA → MPS → CPU walk, `PAPERHUB_DEVICE` override) so GPU operators don't get silent CPU inference. CUDA wheels are opt-in via `uv sync --extra cu124` / `cu126` / `cu130` — the default install stays CPU-only. **v2.8**: the embedder lives in a **separate `paperhub-modelserver` process** (FastAPI + `/embed` endpoint). The Paper Pipeline holds a thin `httpx.Client`-backed `_HttpEmbedder` against the existing `Embedder` Protocol; the in-process `_SentenceTransformersEmbedder` stays available for `PAPERHUB_INPROCESS_MODELS=1` (tests, low-resource hosts). The backend lifespan calls `ensure_running(host, port)` which probes `/health` first and skips the spawn when an instance is already up — making `uvicorn --reload` zero-cost because the previous worker's spawned modelserver outlives the reload via Windows `CREATE_NEW_PROCESS_GROUP` / Unix `start_new_session=True`. The intentional cross-reload leak is the point: weights stay resident, `--reload` only reloads the worker. | New |
| **Render to HTML** | One-shot conversion of the flattened LaTeX / extracted PDF text into a single navigable HTML document; emits `source.html` in the cache dir alongside `source.flattened.tex`; chunk `char_start` / `char_end` offsets index into this HTML so the Citation Canvas (FR-03) can scroll + highlight without re-rendering at click time. Tool: `pandoc` (LaTeX → HTML5) or a minimal `pylatexenc` fallback for malformed arXiv sources; **PDF inputs render from Marker's structured output (HTML/markdown, v2.19)** rather than raw PyMuPDF text. Path written to `paper_content.html_path`. | New |
| **Persistence (content layer)** | Insert one `paper_content` row (with `content_key`, `source_path`, `source_dir_path`, `html_path`, metadata) + N `chunks` rows (FK `paper_content_id`) + N Chroma vectors. **This runs at most once per unique paper across the lifetime of the workspace.** | New |
| **Persistence (membership layer)** | Insert one `papers (session_id, paper_content_id, enabled=true, added_at)` row. This stage runs on **both** the cache-hit and cache-miss paths — it's the only stage UC-2's "add to session" always executes. | New |

### III-5.2 paper_qa retrieval (chunks → answer)

**v2.10 — agentic hierarchical retrieval (replaces the v2.2-v2.9 dense-RAG map-reduce).** The Research Agent's `paper_qa` flow is a four-node LangGraph subgraph: `pq_resolve → {pq_empty | pq_dispatch} → pq_finalize`.

1. **pq_resolve** — `enabled_paper_content_ids = SELECT paper_content_id FROM papers WHERE session_id = :session_id AND enabled = TRUE`. Empty → `pq_empty` returns the "add a reference first" message.
2. **pq_dispatch** — fan-out one **per-paper subagent** per enabled paper via `asyncio.gather`. Each subagent (small-tier model, `PAPERHUB_PAPER_QA_SUBAGENT_MODEL`) runs a bounded tool-calling loop with two tools:
   - `list_sections()` — returns the paper's section TOC from `paper_content.sections_json` (`{name, token_count, chunk_count}` per section). Free; no budget cost.
   - `read_section(name)` — returns every chunk in that section, each headed `[chunk:<id>]`. Counts against `MAX_SECTION_READS=5`.
   The subagent navigates the paper **by its section structure** (not by a global cosine-similarity top-k), picks the chunks that answer the question, and cites them with `[chunk:<id>]` markers in a short summary. Python extracts the cited IDs (`chunk:<digits>` token match, dedup) into a `PerPaperPicks`. If the LLM bails without citing, the best-effort fallback hands every chunk the subagent read to the finalizer.
3. **pq_finalize** — the flagship finalizer (`paper_qa_model`) reads the **raw cited chunk text** from every per-paper subagent (NOT a summary of it) plus each subagent's one-line rationale, and composes the user-facing answer, preserving `[chunk:<id>]` markers for the Citation Canvas. All-empty picks → "checked every enabled reference but none relevant" short-circuit (no finalizer call).

**Why agentic over dense top-k:** academic papers carry explicit section structure (`Method`, `Experiments`, `Data`, …) that a global embedding search flattens. Letting a per-paper LLM read the TOC and choose sections matches the question to the right region far better than cosine similarity on chunk vectors, and giving the flagship finalizer the *raw chunks* (rather than an analyst's lossy prose summary) is the key correctness lever. The dense embedder + cross-encoder reranker (`BAAI/bge-reranker-base`, hosted in `paperhub-modelserver` per §III-6) remain in the codebase — `paper_search` discovery and the `slides` pipeline still use them — but `paper_qa` no longer routes through them.

**Observability (harness eval).** Each subagent writes ONE `paper_qa:subagent` `tool_calls` row whose `result_summary_json` records the full loop state: `chunks_read_ids`, `chunks_cited_ids`, `listed_sections`, per-turn LLM content + tool calls (`llm_turns`), per-tool args + chunk IDs returned (`tool_call_log`), and the final summary text. `pq_finalize` writes a `paper_qa:finalize` row. The whole flow is reconstruct-able from `SELECT * FROM tool_calls WHERE run_id = ?` alone — no instrumentation script needed (CLAUDE.md agent-flow observability policy).

**Migration.** Existing `paper_content` rows ingested before v2.10 carry `sections_json = NULL` + chunks from the broken pre-v2.10 chunker; `paperhub-reingest` re-chunks + re-embeds them (preserving `paper_content.id`) so the subagent's `list_sections` has a usable TOC.

### III-5.3 Slide Pipeline (chunks → deck)

**Framework (v2.18 — deferred decision resolved): Beamer-via-LangGraph.** The v2.1 three-way deferral (Marp / Slidev / Beamer) is settled in favour of **Beamer/LaTeX** because the product target is **conference-ready academic slides**: lossless math (incl. each paper's custom `\newcommand` macros), real `\includegraphics` figures from the papers, and tikz/pgfplots when a genuine vector diagram is needed — Markdown frameworks (KaTeX + Mermaid) are toy-grade for a conference talk. The cost is accepted: **`pdflatex` is now a hard dependency** for the `slides` intent (absence → the agent returns a "install a LaTeX distribution" message, not a crash), and the compile-fix retry loop is load-bearing. The copied `paper2slides-plus` LaTeX helpers become load-bearing rather than dead weight. Default Beamer theme `metropolis` with a built-in fallback (`Madrid`) when the theme package/fonts are absent (non-fatal compile warning).

**Agent topology (v2.21 — three decoupled sub-flows; F4 redesign).** A deck is a **living artifact**: generation, speaker-note authoring, and editing are independent, opt-in operations rather than one bundled run. `report_graph` keeps the single `slides` router intent, but a **deck-command classifier** (small LLM, runs only when a `decks` row already exists for the session) emits a `DeckCommand {action ∈ generate_notes | edit_notes | edit_slides | regenerate, target ∈ current | page:N | all, note_language?}` and routes to one of three sub-flows. No existing deck → **GENERATE**.

- **GENERATE** *(slides only — notes opt-in)* — `sl_resolve → sl_understand → sl_narrate → sl_draft (frame-only) → sl_coherence → sl_assemble → sl_verify_figures → sl_compile → sl_emit`. Produces slides with **no speaker notes**; the finalize message **hints** the next moves (generate notes · edit a slide · edit the deck).
- **NOTES** *(opt-in, separate; `generate_notes` / `edit_notes`)* — `nt_load → nt_author (per page, in `note_language` — independent of the slide language) → nt_finalize → nt_persist → emit`. Writes `deck_slides.note_text` + `note_language`; never touches frames.
- **EDIT** *(diff-edit, scoped; `edit_slides` / `edit_notes` with `target`)* — load the target `deck_slides` row(s) → single targeted LLM rewrite of the frame (or note) → `sl_verify_figures → sl_compile` → persist. Never a full regen. `regenerate` is the explicit "redo the whole deck" → GENERATE.

**Length budget (v2.21).** `sl_narrate` takes a real slide budget: default **20 min ≈ 15 slides** (`slides ≈ minutes × 0.75`, clamped 8–30) + `depth ∈ {overview, standard, deep}`; the user's "20-minute talk" / "15 slides" flows in (was emergent from `pdflatex`).

The PhD-grade single-pass core below (v2.19) is the GENERATE path's stage detail. `agents/report_graph.py` is a traced LangGraph subgraph that simulates how a PhD builds a talk, consuming each paper's **`PaperAsset`** (§III-5.1: figure inventory with captions, equations as LaTeX, structured sections). Cost is no object — flagship throughout, multi-pass. GENERATE nodes:

1. **`sl_resolve`** *(small LLM — the deck-command classifier)* — resolve `enabled_paper_content_ids` (Q&A scope rule); load any existing `decks` row; if one exists, classify the turn into a `DeckCommand` (`generate_notes` / `edit_notes` / `edit_slides` / `regenerate`, with `target` from `current_view_page` or a named page, and an optional `note_language`) and route to NOTES / EDIT / GENERATE accordingly; no deck → GENERATE. Also parse any length intent (target minutes / slide count) into the narrate budget. Empty enabled set → "enable a reference first". `pdflatex` absent → install message.
2. **`sl_understand`** *(per paper, flagship fan-out)* — build a structured brief per paper: core contribution, problem, method, key results, **which figures matter (by `PaperAsset` inventory id)**, key equations (LaTeX). Reads `PaperAsset.structure` + retrieval. This is the "read the paper deeply" step.
3. **`sl_narrate`** *(flagship)* — produce ONE coherent **synthesis** storyline (single paper degrades to a clean summary), sized to the **length budget** (target slide count + depth). Output is a slide-by-slide outline following the `paper2slides-plus` content contract (slide 1 = title / first-author / date / arXiv-url; slide 2 = executive summary; then intro → method [the bulk] → results [quantified] → conclusion): per slide `{title, communicative goal, key points, assigned figure id (from inventory) OR equation (LaTeX), source chunk ids, source paper ids}`. **Figures are assigned here, only from the real inventory** (grounding).
4. **`sl_draft`** *(per slide, flagship fan-out — frame-only, v2.21)* — for each outlined slide, generate a **concise Beamer frame**: ≤~4 bullets (≤~12 words each, quantified claims) + at most the one assigned figure/equation. **Speaker notes are NOT drafted here** (v2.21 decoupling — the NOTES sub-flow authors them on demand). Frames forbid `\pause`/overlays so PDF pages == logical frames.
5. **`sl_coherence`** *(flagship, holistic)* — one pass over ALL drafted frames: remove redundancy, fix flow + transitions, enforce consistent style + density discipline (the reference's `update` stage, smarter).
6. **`sl_assemble`** *(deterministic)* — preamble (`\usetheme{metropolis}` + fallback, `\input{ADDITIONAL.tex}`) + title + frames; merge `ADDITIONAL.tex` macros from each LaTeX-source paper; **stage the assigned figures** into one `slides/figures/` dir under deck-unique keys + a single `\graphicspath` (4a).
7. **`sl_verify_figures`** *(deterministic — HARD no-hallucination guarantee)* — every `\includegraphics{key}` MUST reference a staged inventory key; any unknown → the frame is sent back to a targeted re-draft (`sl_draft` on that frame) or the figure is dropped. **A non-existent figure can never reach the PDF.** Records the verification result.
8. **`sl_compile`** *(LOOP — self-correcting layout)* — `pdflatex`; success requires not just exit-0 but **no Overfull-vbox / off-slide overflow**. On a fatal error OR an Overfull-vbox, a flagship `revise` call rewrites/splits the offending frame(s) with the log → recompile. Iterate (generously bounded — quality over speed). A deck that still won't compile emits `status='error'` + last good PDF (NFR-02).
9. **`sl_emit`** — write/update `decks` + one `deck_slides` row per final frame (`frame_tex`, `page_start`/`page_end`; `note_text` NULL until the NOTES flow runs), snapshot a version, emit the `deck` SSE event + a **hinting** final prose (generate notes · edit a slide · edit the deck). GENERATE emits `has_notes=false`.

**NOTES sub-flow (v2.21 — opt-in; `generate_notes` / `edit_notes`):** `nt_load` (read `deck_slides` + final PDF page map) → `nt_author` (flagship, per page: a **rich** spoken narrative in `note_language` — independent of the slide language; `edit_notes` rewrites existing `note_text` toward the instruction, e.g. re-language to Traditional Chinese) → `nt_finalize` (reconcile to FINAL page count, one note per page, pad/repair gaps) → persist `deck_slides.note_text` + `note_language`, refresh the derived `decks.speaker_notes_json` → emit `has_notes=true`. **Never touches frames** (this is the bug fix: re-languaging notes no longer regenerates slides).

**EDIT sub-flow (v2.21 — diff-edit; `edit_slides` with `target`, detailed in plan F4):** load the target `deck_slides` row(s) by `target` (`current` → `current_view_page`; `page:N`; `all`) → single targeted LLM rewrite of the frame (reuse `beamer_helpers` extract/replace) → splice into the deck `tex` → `sl_verify_figures → sl_compile` → snapshot + update the affected `deck_slides` rows. `regenerate` → GENERATE.

4a. **Figure staging (v2.19 — replaces the v2.18 cross-boundary `\graphicspath` of raw cache dirs).** The v2.18 approach (point `\graphicspath` at each paper's raw cache dir) caused **cross-paper filename collisions** (`img_2` in two papers → wrong image) and Windows-backslash compile failures. v2.19: `sl_assemble` **stages only the assigned figures** from each `PaperAsset` into one `slides/figures/` dir under **deck-unique keys** (e.g. `p{idx}-{stem}`), with a single forward-slashed `\graphicspath{ {.../slides/figures/} }`. `\includegraphics[width=…,height=…,keepaspectratio]{key}` then resolves unambiguously. One `report:figure_stage` `tool_calls` row records the staged key→source map.

**Tracing** (observability policy): `report:resolve`, `report:understand[paper]` (brief + chosen figure/equation ids), `report:narrate` (the outline + per-slide assigned asset ids), `report:draft[slide]` (frame + note + asset id + chunk ids), `report:coherence`, `report:figure_stage`, `report:verify_figures` (unknown-key rejections + redrafts), `report:compile` (**always** records the trimmed log incl. Overfull lines + attempts + page_count — not only on failure), `report:notes_finalize`, `report:emit`. Full reconstruct-from-DB; no rendered prompts.

**Reused from `paper2slides-plus` (copy+adapt, provenance per NFR-03):** `latex_helpers` (defs/packages extraction, `ADDITIONAL.tex`, `sanitize_frametitles`), `compile` (the `compile_with_revise` loop, now Overfull-aware), `beamer_helpers` (frame extract/replace for edits), `history` (`VersionHistory`). **Not reused:** the single-paper orchestrator. **Nuked from F1:** `slides_plan/section/notes` prompts, the `figures.py` flat-stem approach, the `_revise` no-op.

**Persistence + version history.** One **current deck per session** (`decks` table, §III-7, `UNIQUE(session_id)`). Snapshot `(tex, speaker_notes)` bundles under `workspace/chat_session/<sid>/slides/edit_history/version_<ts>.json` after every successful compile; `VersionHistory.list_versions` / `restore_version` (lockstep tex+notes, then recompile to regenerate the PDF). REST: `GET /sessions/{id}/deck`, `/deck/pdf`, `/deck/tex`, `/deck/versions`, `POST /deck/versions/{file}/restore`. SSE: a `deck` event `{deck_id, pdf_url, page_count, title, contributing_papers, has_notes, status}` after `sl_emit`; generation progress streams over the existing `tool_step` events.

### III-5.4 Vector store

**Chroma** (local file backend, under workspace). Houses every chunk emitted by the Paper Pipeline — keyed by `paper_content_id` (v2.2 two-layer split, §III-7), so each unique paper has exactly one set of vectors regardless of how many sessions reference it. Queried by both `paper_qa` retrieval and `slides` per-section retrieval. Metadata filters include `paper_content_id`, `section`, `char_start`, `char_end` so the Citation Canvas can resolve clicks without extra DB hits.

## III-6. ⑤ External Systems & APIs

PaperHub talks to a small set of external surfaces. **There is no `paper2slides-plus` service** — the project is decomposed and its useful pieces are copied in (§III-8). **There is no `arxiv` MCP** — arXiv is reached directly via the adapted Python helper inside the Paper Pipeline (§III-5.1). **v2.5** — `web_search` was reintroduced as `Aas-ee/open-webSearch` after the v2.4 paper-search UX showed weak recall on short / vague queries (the v2.2 omission rationale "no organic use case" no longer holds).

| External system | How PaperHub talks to it | Scope |
| --- | --- | --- |
| `filesystem` MCP | `@modelcontextprotocol/server-filesystem` (npm, CVE-pinned) | Single workspace root; `..` rejected; symlink-escape rejected |
| `sql` MCP *(in-repo, v2.16)* | In-process **FastMCP** sub-app named `sql`, mounted on the FastAPI app at its own path (alongside `paperhub-papers`); reached over loopback by the SQL Agent's `MCPClient`; one `[[server]]` block in `mcp_servers.toml`. Tools: `sql.list_tables`, `sql.describe(table)`, `sql.query(sql)`. Same external-client reach as `/mcp`. | **Read-only.** `sql.query` validated by `sqlglot.parse_one`: verb allowlist `{SELECT, WITH}` + table-name allowlist `{paper_content, papers, chunks, chat_sessions, messages, runs, tool_calls}` (the `memories` table is **excluded** — owned by the `memory` MCP). Out-of-scope verb/table → call rejected + `tool_calls.status='rejected'` (NFR-05; first reachable `rejected` path). Statement timeout + row cap |
| `memory` MCP *(in-repo, v2.16 + v2.17 governance)* | In-process **FastMCP** sub-app named `memory`, mounted at its own path, reached over loopback. The **only write-capable MCP surface** in the system. Tools: `memory.recall(query, scope)` (FTS5 over `memories.content`, filters to `status='active'`), `memory.add(content, scope)` (Memory Gate runs first — rejects sensitive/dangerous content; on allowed content, LLM conflict-detection may supersede an existing active memory), `memory.edit(id, content)`, `memory.forget(id)`. Used by the router `memory` node and (for `add`/`recall`) autonomously by `paper_qa` / `library_stats`. **Curation REST endpoints are separate** (`GET/PATCH/DELETE /memories` in `api/memories.py`) — they are UI-driven deterministic operations (FR-11), not part of the MCP surface; agents do not call them. | **Scope-enforced writes (NFR-05).** `scope='session'` binds to the request-context `session_id`; `edit`/`forget` ownership-checked — target must be `global` or this session's, else rejected + `status='rejected'`. Sole owner of the `memories` table; not visible to the `sql` MCP. `recall` is active-only (v2.17). Memory Gate (v2.17) refuses sensitive/dangerous content before any write, even on user-explicit request. ≤2 autonomous `add`/turn |
| `open-webSearch` MCP *(v2.5)* | [`Aas-ee/open-webSearch`](https://github.com/Aas-ee/open-webSearch) (npm, operator-installed: `npm install -g open-websearch`; runs as a long-lived local daemon via `open-websearch serve`); backend connects via streamable-HTTP at `http://localhost:3000/mcp`. Optional external dependency — same posture as `pandoc`: if absent, the `web.*` tools simply don't enter the Research Agent palette and `paper_search/v1` loads instead of `paper_search/v2`. Tools exposed (allowlisted in `mcp_servers.toml`): `search` (multi-engine: DDG / Bing / Brave / Startpage / Baidu / Sogou) → namespaced as `web.search`; `fetchWebContent` → aliased to `web.fetch` | No API key; discovery-only (results never carry a `paper_id` and cannot enter `json:candidates` — the agent must round-trip through `search_semantic_scholar` for a citable hit). 8-second per-call timeout; multi-engine fallback when individual engines rate-limit; 3-call combined cap with `search_semantic_scholar` per turn (FR-07). Daemon-down behaviour: log WARN, skip the server, continue with v2.4 palette |
| `paperhub-papers` MCP *(v2.6 — in-process)* | In-process **FastMCP** server mounted as a sub-app on the existing FastAPI app at `/mcp` (no extra process, no second port). The Research Agent's `MCPClient` connects over loopback to `http://localhost:<backend_port>/mcp`; external MCP clients (Claude Desktop, Cursor) connect to the same URL — fulfils the §III-6 last-paragraph promise. Tools exposed: `papers.search_library`, `papers.search_semantic_scholar`, `papers.find_related_papers` (same JSON-schemas as the in-process v2.4 dispatchers — zero LLM-contract change). The Python `add_paper_to_session_dispatch` helper **stays in-process** (not in the LLM-visible palette per v2.4; used by FastAPI endpoints, not the agent). Registry connects **lazily** on first tool use to avoid the startup race with uvicorn's bind. | Bound to the same SQLite + Chroma + Semantic Scholar HTTP client the existing in-process dispatchers use today (literally the same code, re-mounted as MCP tools). Tracer-step shape unchanged — every call still writes a `tool_calls` row tagged `paper_search:papers.<tool>`. No new operator install required — ships with the backend |
| `paperhub-modelserver` *(v2.8 — sibling subprocess)* | Separate Python process spawned by the backend lifespan via `paperhub.modelserver.spawn.ensure_running(host, port)` — TCP-probes `/health` first and reuses any running instance, else spawns ONE detached subprocess (Windows `CREATE_NEW_PROCESS_GROUP` / Unix `start_new_session=True`) with `stdout=DEVNULL`. Listens on `PAPERHUB_MODEL_SERVER_HOST/PORT` (default `127.0.0.1:8001`). Exposes three HTTP endpoints: `GET /health`, `POST /embed {texts}` → `{vectors}`, `POST /rerank {query, texts, top_k}` → `{indices, scores}`. The backend's Paper Pipeline + RAG retriever hold thin httpx.Client-backed `_HttpEmbedder` / `_HttpReranker` against these endpoints. | The whole point of the separate process is to **survive `uvicorn --reload` cycles**: the worker's reload kills the worker's process group, but the modelserver is in its own group so it stays up. Weights (~110 MB SentenceTransformer + ~80 MB CrossEncoder) stay resident across all backend edits. The backend lifespan **intentionally does NOT terminate** the subprocess at shutdown — cleanup is OS-reboot / manual `taskkill` / `pkill -f paperhub-modelserver`. `PAPERHUB_INPROCESS_MODELS=1` opts back into the v2.7 in-process singletons (tests, low-resource hosts). `scripts/start.ps1` provides explicit lifecycle for operators who want visible modelserver logs + clean shutdown. Pre-warm in lifespan is fire-and-forget via `asyncio.create_task` so HF Hub cold-cache downloads don't block startup |
| `marker` *(in-repo, v2.19 — docker-compose service; **optional async add-on, v2.20**)* | A FastAPI wrapper around [`datalab-to/marker`](https://github.com/datalab-to/marker)'s `PdfConverter`, run as a **docker-compose service** (the first of the project's planned full-Docker move; `docker-compose.yml` at repo root). `POST /extract` (PDF bytes + optional `page_range`) → structured JSON (blocks with `section_hierarchy`; `BlockTypes.Figure/Picture` cropped images + captions + bbox; `BlockTypes.Equation` LaTeX; tables). Models bake into the image; GPU-optional. **v2.20 posture: a preferred-when-present add-on, NEVER on the synchronous ingest path.** PDF ingest returns immediately on the PyMuPDF baseline (§III-5.1); a single sequential **background worker** calls `/extract` to upgrade `marker_pending` papers (re-extract → upgrade `PaperAsset` + re-chunk/re-embed → `marker_ready`), `PAPERHUB_MARKER_MAX_PAGES` default 1 (VRAM scales with page content density, not count — a dense two-column page = 200+ OCR lines). Service unreachable → ingest stays `pymupdf_only` and the app is fully functional; the worker resumes pending rows when the service returns. arXiv e-print papers never use it (LaTeX path). Client read timeout 1800s. | Output cached as `PaperAsset` in `papers_cache/<key>/asset/` so a re-add is a cache-hit. Heavy model weights; cold start downloads on first build. `paperhub-backfill-assets` backfills pre-F2 papers (sequential) |
| LLM provider APIs | LiteLLM adapter (Gemini / Anthropic / OpenAI / Ollama) | API key required; redacted in `tool_calls.args_redacted_json` |
| arXiv API *(not an MCP)* | `arxiv` Python client invoked from inside the Paper Pipeline | Domain pinned to `arxiv.org`; per-ToS rate limit honoured by the client |

A **server-side** `paperhub.*` MCP surface exposes `get_paper`, `search_library`, `list_runs`, `get_trace` so external MCP clients (Claude Desktop, Cursor) can drive PaperHub through the same tracer.

### III-6.1 MCP client layer *(v2.5 — ships in Plan C as course-correction)*

Plan C originally expected to ship a `paperhub-papers` external MCP server wrapping the Research Agent's three in-process dispatchers (`search_library`, `search_semantic_scholar`, `find_related_papers`); the actual Plan C implementation took a shortcut and kept those tools in-process. v2.5 **does not retroactively wrap them** — that work is accepted technical debt, deferred to Plan E alongside the sqlite MCP server — but it **does** ship the **MCP client infrastructure** that should have been there from day one, with `open-webSearch` as the first consumer. The same client layer is reused by Plan E (sqlite MCP) and Plan G (filesystem + custom `paperhub.*` MCP) without code changes — they each add one `[[server]]` block to `mcp_servers.toml` and inherit everything else.

Shape:

- **`paperhub.mcp.client.MCPClient`** — one connector per configured server. Owns the streamable-HTTP transport (`mcp.client.streamable_http.streamablehttp_client` from the official Anthropic `mcp` Python SDK), the cached `tools/list` response, and the dispatcher. Idempotent connect/disconnect; reconnect with exponential backoff (cap 4 attempts) when a tool call surfaces a transport error.
- **`paperhub.mcp.registry.MCPRegistry`** — process-wide singleton attached to FastAPI's `app.state.mcp_registry` via the lifespan hook. Loads server configs from `backend/mcp_servers.toml` at startup, calls `MCPClient.connect()` for each, aggregates their tool schemas under a per-server namespace (`web.search`, `web.fetch`; future: `sql.query`, `fs.read_file`, `paperhub.get_paper`). Unreachable servers log WARN and are skipped — the backend continues with whatever reachable servers advertised.
- **`backend/mcp_servers.toml`** *(gitignored; checked-in template `mcp_servers.toml.example`)* — per-server config: `name` (namespace prefix), `transport` (`streamable_http` | `stdio`), `url` or `command`+`args`, `expose` (allowlist of upstream tool names), optional `aliases` (rename `fetchWebContent` → `fetch`), `timeout_seconds`. Plan E adds two blocks (`sql` + `memory`, v2.16); Plan G adds two more (filesystem + paperhub.*).
- **Routing**. `research_tools.build_tool_schemas(mcp_registry)` returns the base 3 in-process schemas + `mcp_registry.aggregate_tool_schemas()`. `research._dispatch_paper_search_tool_call` branches on `"." in tool_name` — namespaced names route through `mcp_registry.call(...)`, others stay on the in-process dispatchers. The tracer step name becomes `paper_search:<namespaced_name>` (e.g. `paper_search:web.search`) so existing ToolStrip rendering picks it up unchanged. Errors (timeout, transport failure, upstream tool error) are wrapped as `MCPUnavailableError` / `MCPToolError` and surfaced to the LLM as `{"error": "..."}` results so it falls back to the in-process palette rather than aborting the turn.
- **Dependency**. `mcp>=1.0` added to `backend/pyproject.toml`. The npm-side dependency (open-webSearch + Node ≥ 18) is operator-installed and documented in CLAUDE.md alongside `pandoc`.

~~The deliberate scope cut is that **retroactive MCP-wrapping of the existing in-process Research Agent tools** stays as Plan E work — bundling it with the sqlite MCP server (which Plan E was already going to add) is the natural moment to stand up a `paperhub-papers` FastMCP server too. v2.5 ships just enough to consume external servers; making PaperHub *provide* one stays a Plan E + Plan G concern.~~

**v2.6 rescinds this deferral.** Keeping the dual dispatch path (in-process `papers.*`, MCP-client `web.*`) re-introduces the exact "manual tool" pattern the v2.5 course-correction set out to eliminate, just dressed up. v2.6 pulls the `paperhub-papers` FastMCP server into Plan C v2.5 scope: a FastMCP sub-app is mounted on the existing FastAPI app at `/mcp` (no extra process, no second port), and the Research Agent's `MCPClient` reaches it over HTTP loopback. Every tool the agent has — `papers.*` and `web.*` — now flows through one dispatch path. The external `paperhub.*` MCP surface promised in §III-6's last paragraph (Claude Desktop / Cursor reach) comes along for free at the same URL. Plan E's residual scope after this is the **`sql` + `memory` MCP servers** (v2.16) — both in-process FastMCP sub-apps in the same shape as `paperhub-papers`. See §III-6's `paperhub-papers` row above for the operational shape; Plan C v2.5 follow-up Task v2.5-3 builds it.

## III-7. ⑥ Database — Schema + On-Disk Layout

Nine tables in one SQLite file. Papers are **session-scoped** (each `papers` row carries a `session_id`) — this mirrors the on-disk tree below where every imported paper lives under the chat session that imported it.

```sql
chat_sessions  (id, created_at, title)
paper_content  (id, content_key UNIQUE, kind, arxiv_id NULL, sha256 NULL,
                title, authors_json, year, abstract,
                source_path, source_dir_path, html_path,
                sections_json NULL,   -- v2.10: section TOC for paper_qa subagent
                asset_status NULL,    -- v2.20: 'latex'|'pymupdf_only'|'marker_pending'|'marker_ready'|'marker_failed'
                ingested_at)
papers         (id, session_id → chat_sessions.id,
                paper_content_id → paper_content.id,
                enabled, added_at,
                UNIQUE (session_id, paper_content_id))
chunks         (id, paper_content_id → paper_content.id,
                section, char_start, char_end, text)
messages       (id, session_id → chat_sessions.id, role, content, run_id, created_at)
runs           (id, session_id, routing_decision_json,
                started_at, finished_at, status)
tool_calls     (run_id → runs.id, branch DEFAULT '', step_index, parent_step,
                agent, tool, model,
                args_redacted_json, result_summary_json, latency_ms,
                token_in, token_out, status, error,
                PRIMARY KEY (run_id, branch, step_index))
memories       (id, scope CHECK (scope IN ('session','global')),   -- v2.16 (FR-10)
                session_id NULL → chat_sessions.id,                -- NULL ⇔ scope='global'
                content, created_at, updated_at,
                status TEXT DEFAULT 'active'                       -- v2.17: 'active'|'superseded'
                    CHECK (status IN ('active','superseded')),
                supersedes INTEGER NULL,                           -- v2.17: id this memory replaced
                superseded_by INTEGER NULL)                        -- v2.17: id that replaced this
decks          (id, session_id → chat_sessions.id,                 -- v2.18 (FR-12); one per session
                run_id NULL → runs.id,
                tex_path, pdf_path NULL, speaker_notes_json NULL,  -- v2.21: derived cache of deck_slides.note_text
                plan_json NULL, page_count, theme,                 -- theme default 'metropolis'
                contributing_paper_ids_json,
                status DEFAULT 'ok' CHECK (status IN ('ok','error')),
                created_at, updated_at,
                UNIQUE (session_id))
deck_slides    (id, deck_id → decks.id ON DELETE CASCADE,          -- v2.21 (FR-12); one row per final frame
                slide_index,                                       -- logical frame order
                frame_tex,                                         -- the \begin{frame}…\end{frame} block
                note_text NULL,                                    -- NULL until the NOTES sub-flow runs (opt-in)
                note_language NULL,                                -- independent of the deck/slide language
                page_start, page_end,                              -- PDF pages this frame spans
                UNIQUE (deck_id, slide_index))
```

**Two-layer design (v2.2)** — `paper_content` is the *physical* layer: one row per unique paper (keyed by `content_key`, where `arxiv` papers use `arxiv:<arxiv_id>` and uploads use `sha256:<hex>`), owning the on-disk artefacts and **all** `chunks` + Chroma vectors. `papers` is the *logical* / session-membership layer: one row per (session, paper) pair, carrying only the toggle (`enabled`) and `added_at`. Re-importing a paper into another session inserts a new `papers` row pointing at the existing `paper_content_id` — no re-download, no re-extraction, no re-chunking, no re-embedding (UC-2, §III-5.1).

- `papers.session_id` + `papers.enabled` together drive FR-08 (session-scoped reference sources with toggle).
- `paper_content.content_key` is the cache lookup key: `kind='arxiv'` → `content_key='arxiv:<arxiv_id>'`; `kind∈{'pdf_upload','latex_upload'}` → `content_key='sha256:<hex>'`. Exactly one of `arxiv_id` / `sha256` is populated per row (CHECK constraint).
- `chunks` hang off `paper_content_id`, NOT `papers.id` — chunks are content-layer, deduped across sessions.
- `chat_sessions` + `messages` back the chat history.
- `runs` + `tool_calls` back the audit trail (FR-09).
- `memories` (v2.16 + v2.17) backs FR-10 and FR-11 — session-scoped (`session_id` set) or global (`session_id` NULL) facts. `scope='session'` is project-scope (a fact tied to this chat/project context, e.g. "this project uses FastAPI"); `scope='global'` is user-scope (a personal, cross-session preference, e.g. "always answer in Traditional Chinese"). These two enum values ARE the functional user/project distinction — no rename needed. `status` (`active`/`superseded`) + `supersedes`/`superseded_by` ids track the conflict-supersede lifecycle (v2.17); only `status='active'` rows are recalled by agents or injected as context. The `memory` MCP is the sole write surface for agents; `api/memories.py` REST endpoints are the curation surface for the UI (FR-11). Owned exclusively by the `memory` MCP for agent use (the read-only `sql` MCP's table allowlist deliberately excludes it). FTS5 over `content` powers `memory.recall`. The v2.17 status/supersedes/superseded_by columns are added via an idempotent column-add migration.
- `decks` (v2.18) backs FR-12 + UC-4 — one **current** deck per session (`UNIQUE(session_id)`). Holds the tex/pdf paths, `speaker_notes_json` (`{page: note}` — **v2.21: a derived cache** of `deck_slides.note_text`, kept for the `deck` SSE `has_notes` flag + the `GET /deck` back-compat shape), the `plan_json` outline (for edit reasoning), `page_count`, Beamer `theme`, and `contributing_paper_ids_json` (which `paper_content` rows the deck draws from). Version history is **file-based** (snapshot `(tex, speaker_notes)` bundles under `chat_session/<sid>/slides/edit_history/`, parity with `paper2slides-plus/src/history.py`), not a table. Added via an idempotent migration.
- `deck_slides` (v2.21) backs the decoupled slide/note operations (FR-12) — one row per **final** frame `(deck_id, slide_index)`, holding the frame's LaTeX, its (opt-in, possibly differently-languaged) speaker note, and the PDF page span it occupies. This is the per-slide granularity that lets the NOTES sub-flow author/re-language notes and the EDIT sub-flow diff-edit a single frame **without re-serializing the whole deck** — the fix for "re-languaging the notes regenerated the slides". `decks.speaker_notes_json` is rebuilt from these rows on every notes/edit write. Added via an idempotent migration alongside `decks`.
- `paper_content.html_path` + `chunks.text` back the Citation Canvas (FR-03): the canvas loads the HTML and text-searches it for the chunk's stored `text` (v2.13 — `char_start`/`char_end` index the extracted text, which only aligns with the `<pre>` fallback renders, not the pandoc DOM, so resolution is by content). `char_start`/`char_end` remain useful for RAG provenance + ordering, not for canvas scrolling.
- `tool_calls.branch` discriminates the two halves of a Compare-mode turn under a single `run_id` (FR-04). Empty string for normal turns; `'A'` or `'B'` inside Compare. PK is `(run_id, branch, step_index)` so step indices may legitimately repeat across branches.
- Cache GC policy: **never** (v2.2). `paper_content` rows + their on-disk cache dirs grow monotonically; orphaned content (no referencing `papers` row) is left in place. The single demo workload is small (≤ tens of papers, < 1 GB).
- No `projects`, no `tags`, no `citations` graph, no `session_references` join — drop on sight if anything else accumulates here without an FR backing it. (`memories` is the one v2.16 addition, and it has FR-10 backing it — note-like tables are otherwise still unwelcome.) Re-importing the same `arxiv_id` in a different session creates a new `papers` row but reuses the existing `paper_content` row (no duplicate physical artefacts).

**On-disk layout (v2.2)** — split between a **shared content cache** (`papers_cache/`, keyed by `content_key`) and **per-session output** (`chat_session/<session_id>/`, slides only). Paper sources no longer live under the session tree.

```
workspace/
├── paperhub.db                       # SQLite — all nine tables
├── chroma/                           # Chroma chunk-vector index (per paper_content.id)
├── papers_cache/                     # shared physical layer — one entry per paper_content row
│   ├── arxiv/
│   │   └── <arxiv_id>/               # for paper_content rows with kind='arxiv'
│   │       ├── source/               # unpacked e-print archive (figures + bib + .tex)
│   │       │   └── main.tex          # paper_content.source_path
│   │       ├── source.flattened.tex  # for RAG chunking
│   │       └── source.html           # paper_content.html_path (FR-03)
│   └── upload/
│       └── <sha256>/                 # for paper_content rows with kind∈{pdf_upload,latex_upload}
│           ├── source/               # original archive (LaTeX upload) or source.pdf (PDF upload)
│           ├── source.flattened.tex  # LaTeX uploads only
│           └── source.html           # paper_content.html_path
└── chat_session/
    └── <session_id>/                 # one per chat_sessions.id — slides only, no paper sources
        └── slides/                   # v2.18 — the session's one current Beamer deck (decks table)
            ├── deck.tex              # decks.tex_path
            ├── deck.pdf              # decks.pdf_path (NULL until first successful compile)
            ├── ADDITIONAL.tex        # merged+deduped macros from all contributing LaTeX-source papers
            ├── speaker_notes.json    # {page: note}
            └── edit_history/         # version snapshots — version_<YYYYMMDD_HHMMSS>.json
                                      #   {tex_content, speaker_notes, description, timestamp}
```

The Slide Pipeline produces **one current Beamer deck per session** (`deck.tex` + `deck.pdf`, framework locked v2.18 — §III-5.3) that references any subset of the enabled papers in that session — the multi-paper synthesis contract from UC-4. Editing/recreating overwrites `deck.tex`/`deck.pdf` but **snapshots the prior `(tex, speaker_notes)` into `edit_history/` first** (version-revert parity with `paper2slides-plus`); figures are never copied here — `\graphicspath` reaches into `papers_cache/...` (§III-5.3 step 4a).

**Datastore choices:**
- **SQLite** — sole relational datastore (nine tables). (DuckDB was evaluated as an opt-in OLAP path and dropped in v2.16 — the demo fixture set is small enough that plain SQLite handles every `library_stats` query, so a second engine was dead scope.)
- **Chroma** — file-backed under `workspace/chroma/`.
- **Local FS** — split between `workspace/papers_cache/{arxiv,upload}/<key>/` (shared per `paper_content` row) and `workspace/chat_session/<session_id>/` (session-scoped slide output only) per the tree above.

## III-8. Reference-repo integration contract

**Single posture: direct-copy with provenance.** Neither reference project is run as a service. Useful code is copied into the new tree and edited as needed.

- **What to copy.** Utility functions, prompt templates, regex helpers, React components, CSS, MCP-tool wrappers — anything where a service boundary is overkill.
- **What NOT to copy verbatim.** Top-level orchestrators (`paper2slides-plus/src/core.py:generate_slides`, the `chat-with-paper` route, the Node backend of `Intro2GenAI-hw1`) — these are rebuilt in the new LangGraph / FastAPI / React shape.
- **Provenance rule.** Every copied file preserves the original LICENSE / copyright header at the top and adds a single comment: `# Adapted from reference/<repo>/<path> @ <commit>`. Greppable; a future reader can always find the source.
- **`paper2slides-plus` reuse, concretely.** Copy + adapt: `arxiv_utils.py` (arXiv search + e-print download), `pdf_utils.py` (PDF text extraction), `latex_utils.py` (preamble strip, section split, `\includegraphics` rewrite), `file_utils.py` (workspace helpers), individual prompts from `prompts/config.yaml` where they apply. **Skip:** the chat-with-paper orchestrator (can't expose chunk-level citation IDs for the Canvas), `core.py:generate_slides` chain (rebuilt as a LangGraph node graph for per-step tracing).
- **`Intro2GenAI-hw1` reuse, concretely.** Copy + adapt: SSE parsing loop, thinking-block disclosure CSS + DOM logic, history-sidebar layout, fork-from-message UX, MCP indicator chip styles. Re-implement in React (the source is vanilla JS, but the layout grammar and CSS variables port cleanly). **Skip:** the Node + Express backend; PaperHub's FastAPI orchestrator replaces it.

## III-9. Closing principle

When in doubt: **add UX polish before adding backend logic.** When tempted to add a new backend feature, check whether a utility in `reference/paper2slides-plus/` already does it (copy + adapt). When tempted to add a new agent, check whether a single MCP tool would suffice. When tempted to add a new schema table, push back hard.
